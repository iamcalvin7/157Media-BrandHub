export interface ChangelogEntryStatic {
  sortKey: string;
  date: string;
  category: string;
  summary: string;
  capabilities: string[];
}

// Virtu Ferries (brand_id 1) — full operational + strategy knowledge.
export const knowledgeChangelog: ChangelogEntryStatic[] = [
  {
    sortKey: "2026-05-18-f",
    date: "2026-05-18",
    category: "Improvement",
    summary: "Made client feedback visible at a glance on the Content Calendar. Previously the team had to open every post's detail modal to check whether a client had approved or commented via a share link. Each post row in the calendar now shows up to three small coloured pills next to the caption / drive icons — green check for approvals, amber alert for change requests, sky-blue speech bubble for comments — with a count when there is more than one of the same kind. The CardDetailModal still shows the full chronological timeline; the pills are just the at-a-glance summary.",
    capabilities: [
      "`PostRow` in `artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx` reads `post.client_feedback ?? []`, buckets entries into approved / changes_requested / pure-comment, and renders a pill per non-zero bucket. Counts are only shown when the bucket has more than one entry to keep the row compact. Tooltips spell out the exact wording (e.g. '2 client comments').",
      "Data path is unchanged: `GET /api/content/posts` already LEFT-joins `share_post_feedback` (added in 2026-05-18-e), so the pills appear automatically the moment the calendar refetches the month — no schema or API changes needed.",
    ],
  },
  {
    sortKey: "2026-05-18-e",
    date: "2026-05-18",
    category: "Feature",
    summary: "Three connected improvements on the client review flow: (1) posts can now carry multiple photos/videos instead of just one — uploads from the card-detail modal append to a `media_urls` array; (2) the 'Visual reference' link is no longer shown on the public share page, since it's an internal briefing field; (3) clients reviewing a share link can approve a post, request changes, or leave a comment, and every submission flows back into the calendar's CardDetailModal so the team sees decisions and notes alongside the post itself.",
    capabilities: [
      "Schema: added `media_urls jsonb NOT NULL DEFAULT '[]'` to `content_posts` and a new `share_post_feedback` table (`id, share_token, brand_id, post_id, decision, comment, client_name, created_at`) with indexes on `post_id`, `brand_id`, and `share_token`. Both live in `lib/db/src/schema/contentPosts.ts` and the new `lib/db/src/schema/sharePostFeedback.ts`. Pushed to dev via `pnpm --filter @workspace/db run push`. To make prod pick up the schema without a manual push, `artifacts/api-server/src/lib/bootstrapFromSnapshot.ts` now runs an `applyIdempotentMigrations()` pass on every boot — `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` for `media_urls` and `CREATE TABLE IF NOT EXISTS` + index DDL for `share_post_feedback`, all safe to re-run.",
      "Multi-photo posts: `media_url` is kept in sync with `media_urls[0]` so existing readers (calendar cards, briefs, snapshot bootstrap) don't break. The PATCH `/api/content/posts/:id` handler accepts a `media_urls: string[]` body field, normalises it (trim, de-dupe, drop non-strings), writes the array, and mirrors the first entry to `media_url`. `CardDetailModal` now renders a vertical stack of all attachments with per-photo Remove and a single 'Add another' button; the dropzone is still shown when the post has zero media. The local `mediaList` state is the immediate source of truth so the UI updates without waiting for a refetch.",
      "Share page cleanup: `artifacts/virtu-ferries-brand-hub/src/pages/share-view.tsx` no longer renders the 'Visual reference' chip or embeds `visual_reference_url`, and `artifacts/api-server/src/routes/shares.ts` no longer ships that field in the public payload — the field is an internal designer-brief input, not something the client should see. The share payload now includes a `media_urls: string[]` per post (combined from the new column + legacy `media_url`), so multi-photo posts stack vertically in the public view too. The PDF export iterates the full list and labels each entry 'View visual 1', '… 2', etc.",
      "Client feedback: new `POST /api/shares/:token/feedback { postId, decision?, comment?, clientName? }` writes to `share_post_feedback`. `decision` is constrained server-side to `'approved' | 'changes_requested'`, `comment` is capped at 2000 chars and `clientName` at 100, and the postId must be in the share's `post_ids` array — anyone with the link can submit, but they can't leave feedback against unrelated posts. `GET /api/shares/:token` returns a `feedback: ClientFeedback[]` array per post (oldest-first). The share view renders a per-post `FeedbackPanel` with Approve / Request changes pills, an optional comment textarea, an optional name input (persisted to `localStorage` so repeat reviewers don't retype it), and a 'Feedback timeline' above the form showing every prior submission. Submitting optimistically appends the entry into the local payload — no full reload needed.",
      "Admin surface: `GET /api/content/posts` now LEFT-joins `share_post_feedback` for every post in the requested month/brand and returns a `client_feedback: ClientFeedback[]` array on each row. `CardDetailModal` renders a new 'Client feedback' card right under the existing internal-approval card, with count chips (approved / changes / comments) at the top and a chronological list of entries (each with decision badge, reviewer name, and timestamp). Feedback is per-brand and per-post by token, so a single post that's been included in multiple share links surfaces every reviewer's input in one place.",
      "Snapshot policy: `share_post_feedback` is intentionally NOT added to `bootstrapFromSnapshot.TABLES` / `scripts/src/dump-data-snapshot.ts`. It's runtime data submitted on production share links — dev should never overwrite live client feedback. Same policy already used for `approval_decisions`, `conversations`, and `messages`.",
    ],
  },
  {
    sortKey: "2026-05-18-d",
    date: "2026-05-18",
    category: "Polish",
    summary: "Brought the new card-detail inline media uploader in line with the Gozo Highspeed brand colors. The Replace button, dropzone hover state, and uploading spinner now use GHS navy (`#1d3289`) when the active brand is Gozo Highspeed, matching the rest of the GHS UI; Virtu Ferries continues to use its existing brand blue (`#1e82b4`). The underlying size limits (25 MB images / 200 MB videos) and upload flow are already brand-agnostic, so GHS has had functional uploads since 2026-05-18-b — this is purely a visual fit-and-finish pass.",
    capabilities: [
      "`CardDetailModal` (`artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx`): the Replace action, the dashed dropzone (hover border + hover tint), and the in-flight spinner are now wrapped in `isVirtu ? '#1e82b4' : '#1d3289'` ternaries — mirroring the same convention `NewPostModal` already uses for its 'Upload' pill, dropzone, and spinner so the two surfaces stay consistent.",
      "No behavioural changes: object-storage flow, in-flight guard, error surfacing, and the `MAX_IMAGE_BYTES` / `MAX_VIDEO_BYTES` caps on both client and server are unchanged. Remove (red), error message (red), and neutral surface colors (#E4E4E7 / #FAFAFA / #71717A) are also unchanged because they're brand-neutral.",
    ],
  },
  {
    sortKey: "2026-05-18-c",
    date: "2026-05-18",
    category: "Improvement",
    summary: "Media uploads now have explicit size limits — 25 MB for images, 200 MB for videos — enforced both in the browser (instant feedback before the upload starts) and on the API server (so the bucket never receives an oversized file). Previously there was no cap anywhere, which meant huge videos would upload slowly and fail at the object-storage layer with no friendly message.",
    capabilities: [
      "Server: `POST /api/storage/uploads/request-url` (`artifacts/api-server/src/routes/storage.ts`) now reads `size` from the request body and rejects with HTTP 413 + a human-readable error (`File too large — videos must be under 200 MB.`) when it exceeds the cap for the declared `contentType`. Caps live in `MAX_IMAGE_BYTES` / `MAX_VIDEO_BYTES` module constants right above the handler so they're easy to retune. Non-video content types fall back to the image cap.",
      "Client: added a shared `validateUploadSize(file)` helper at the top of `artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx` (next to the `API` constant) with the same 25/200 MB constants. Both upload entry points use it: `NewPostModal.handleFileChange` surfaces the message in its existing `error` strip and resets `selectedFile` / `uploadProgress`, and `CardDetailModal.uploadMedia` surfaces it in `mediaUploadError` and clears the hidden file input so the user can pick a smaller file straight away.",
      "Sync rule: the two caps are duplicated by design (server is the source of truth, client mirrors it for UX). A comment in `storage.ts` and one in `content-calendar.tsx` cross-reference each other so future edits keep them in lockstep.",
    ],
  },
  {
    sortKey: "2026-05-18-b",
    date: "2026-05-18",
    category: "Feature",
    summary: "You can now upload an image or video to a post directly from its card-detail panel — no need to open the full Edit Post modal anymore. The existing Media section turns into a dropzone when the post has no attachment, and gains Replace / Remove actions when it does.",
    capabilities: [
      "`CardDetailModal` (`artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx`): added inline upload state (`mediaUploading`, `mediaUploadError`, hidden file input ref) and an `uploadMedia(file)` helper that hits the same `/api/storage/uploads/request-url → PUT → PATCH media_url` flow the New Post modal uses, so storage handling, brand scoping, and accepted MIME types stay consistent across the two surfaces.",
      "UI: when `post.media_url` is empty the Media block renders a dashed dropzone (image + film icons, 'Click to upload an image or video', accepts JPG/PNG/GIF/MP4/MOV/WebM). Click → native picker → progress spinner → preview as soon as the PATCH lands. When media is already attached the existing preview gets a Replace (refresh icon) and Remove (trash icon) action row in the header. Upload errors surface as a small red message under the dropzone.",
      "Refresh: a tiny `forceMediaRender` counter is bumped after successful upload/remove so the `isImage`/`isVideo` derivations (computed from `post.media_url` higher up in the component) re-evaluate immediately — `Object.assign(post, patch)` mutates the same object reference and React wouldn't otherwise re-render the preview block.",
    ],
  },
  {
    sortKey: "2026-05-18-a",
    date: "2026-05-18",
    category: "Feature",
    summary: "The client share page (`/share/:token`) now actually shows the post visuals — every embeddable image or video attached to a post renders inline, and the inspiration FB/IG reference URL plus the live posted URLs are exposed as labeled link chips. Previously the share page only had `media_url`, `link_url`, and `drive_url`, so posts whose visual lived in `visual_reference_url` (the imported FB/IG share URL field) appeared empty to the client.",
    capabilities: [
      "API (`artifacts/api-server/src/routes/shares.ts`): added `visual_reference_url`, `posted_url`, and `posted_url_ig` to the public `GET /api/shares/:token` payload. These were intentionally stripped before; the team now controls what's shown by populating those fields per-post in the calendar, and they flow through the existing brand ownership check so a token only exposes its own brand's posts.",
      "Share view (`artifacts/virtu-ferries-brand-hub/src/pages/share-view.tsx`): added an `isImage` / `isEmbeddableMedia` helper and a media-collector that walks `media_url → visual_reference_url → drive_url`, deduplicates URLs, and renders each embeddable image (`png/jpe?g/gif/webp/avif/bmp/svg`) or video (`mp4/webm/mov/m4v`) as an inline `<img loading='lazy'>` or `<video controls>`. Non-embeddable URLs (Facebook/Instagram share permalinks, Drive folders, generic links) fall through to the chip row below — never duplicated as both media and chip.",
      "Link chips: replaced the old `Linked URL` / `Drive folder` pair with a generalised chip row that includes `Visual reference`, `Linked URL`, `Drive folder`, `View live post`, and `View on Instagram`. Each chip is `safeUrl`-sanitised (http(s) only — blocks `javascript:` / `data:`), includes the bare domain (`facebook.com`, `drive.google.com`) as a subtle suffix so the client can tell at a glance where the link goes, and `target='_blank' rel='noopener noreferrer'`.",
      "PDF export: extended `downloadAsPdf` to include the three new fields in the per-post `Visual / links` block. Each new URL is written as a sanitised clickable `textWithLink`, so the offline PDF stays in sync with what the client sees in the browser.",
    ],
  },
  {
    sortKey: "2026-05-16-i",
    date: "2026-05-16",
    category: "Feature",
    summary: "Expanded the caption-writer emoji picker to the full Unicode CLDR set (~3,700 emoji) with a searchable, tabbed UI. Search is global and instant (type 'pizza', 'wave', 'fire', 'malta'…); the curated travel/ferry Suggested set stays as the first tab so the team's most-used emoji are one click away.",
    capabilities: [
      "Data: pulled in `unicode-emoji-json` (`data-by-group.json`, ~265 KB inlined by Vite) so the picker is backed by the authoritative Unicode list with searchable names and slugs, instead of a hand-maintained chip list.",
      "Search: built a flat search index at module load (name + slug + group name, lowercased) and a small AND-token matcher. Multi-word queries narrow results (e.g. 'red heart', 'partying face'). Capped at 240 hits with a '+' indicator so very broad queries stay snappy.",
      "Tabs: added a horizontal category strip — Suggested (curated Virtu set) + the nine official Unicode groups (Smileys & Emotion, People & Body, Animals & Nature, Food & Drink, Travel & Places, Activities, Objects, Symbols, Flags). Only the active tab's grid is mounted, keeping render cost low even with the much bigger dataset.",
      "UX: search input auto-focuses when the picker opens; Escape clears a non-empty query first, then closes the picker on the next press. Popover bumped to 320×360 px with portaled fixed positioning so it still escapes modal scroll containers. Inserts continue to land at the cursor with caret restoration; chained picks read from a ref so fast clicks never overwrite earlier insertions.",
    ],
  },
  {
    sortKey: "2026-05-16-h",
    date: "2026-05-16",
    category: "Feature",
    summary: "Added an emoji picker next to **Bold selection** in the Content Calendar's caption writer. Sits in both places captions are edited — the inline card-detail editor and the New Post modal — so emoji can be inserted at the cursor without leaving the field or pasting from a system picker.",
    capabilities: [
      "UI (`artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx`): new `EmojiPickerButton` component renders a `Smile`-icon button matching the Bold-selection styling. Clicking opens a popover with curated travel/Virtu emoji. Closes on outside click or Escape; stays open between picks so the team can chain several emoji in a row.",
      "Insertion: new `insertAtTextareaCursor` helper drops the chosen emoji at the textarea's current cursor (or replaces the current selection) and places the caret immediately after, so typing continues exactly where you left off — emoji never get appended to the end of the field.",
      "Wiring: the picker is bound to the `Caption` field in the New Post / Edit Post modal and to any `Editable` textarea that already had `withBoldButton` (the inline caption editor in the card-detail modal). No backend changes.",
    ],
  },
  {
    sortKey: "2026-05-16-g",
    date: "2026-05-16",
    category: "Knowledge",
    summary: "Added the full Pillar Classification System to the Social Strategy page (`/social-media`) for Virtu Ferries. The five pillars (The Virtu Experience, Choose Sicily / Choose Malta, Virtu Recommends, For the Feed, Flexible / Operational) each get a collapsible card with purpose, when to use, the full list of content that fits, canonical example posts, a one-sentence decision test, and any pillar-specific rules. Cross-cutting categories (Offers, Excursions, Partner/Commercial, Seasonal Campaigns, Customer Education, Testimonials, Pozzallo) are shown as a supporting 2-column note grid below the pillars. Same content is also injected into the agent prompt so the AI classifies ideas the same way the team does.",
    capabilities: [
      "Types: added `PillarClassification`, `ClassificationNote`, and `PillarClassificationSystem` to `lib/brand-knowledge/src/index.ts`, plus an optional `pillarClassification` field on `SocialMediaContent`. Kept it optional and additive so existing brands (including Gozo Highspeed) are unaffected.",
      "Data: populated the full Virtu Ferries classification system from the source brief — purposes, use-when guidance, content-that-fits lists, example posts, the simple test for each pillar, and the cross-cutting notes for offers, excursions, partner content, seasonal campaigns, customer education, testimonials, and Pozzallo.",
      "UI (`artifacts/virtu-ferries-brand-hub/src/pages/social-media.tsx`): new section `04 Classification` slotted between Pillars and Tone. Subsequent sections renumbered (Tone 04→05, Recurring 05→06, Seasonal 06→07, Cross-posting 07→08) and the sticky in-page nav picks them up automatically. Each pillar is a `<details open>` accordion with a chip-grid of fit topics, a bulleted list of examples, a highlighted Simple Test callout in the brand-primary colour, and optional rules.",
      "Prompt: `lib/brand-knowledge/src/prompt.ts` now serialises the same data into the agent's system prompt — pillars with their fits/examples/decision test/rules, plus the cross-cutting notes — so the AI uses the same classification logic the team sees on screen. The standard `formatBrandKnowledgeAsPrompt` filter for scaffold strings keeps this out of empty brands.",
    ],
  },
  {
    sortKey: "2026-05-16-f",
    date: "2026-05-16",
    category: "Feature",
    summary: "Added a Pillar selector to the Saved-for-Later add/edit modal so any saved idea can be linked to one of the active brand's content pillars. The pillar is stored as a reserved `pillar:<name>` tag on the existing `tags` column, so it appears as a `#pillar:<name>` chip on the card, shows up in the Tags rail filter, and matches free-text search — no schema change needed.",
    capabilities: [
      "Frontend (`artifacts/virtu-ferries-brand-hub/src/pages/saved-items.tsx`): added a single-select Pillar `<select>` to `ItemModal`, populated from `usePillars().allPillars` and disabled while the hook is loading. The initial value is derived from the existing item's tags via `extractPillar()` so editing an item preserves its current pillar.",
      "Storage convention: pillars piggy-back on the `tags` column as `pillar:<lowercase-name>`. On submit, the dropdown is the source of truth — `stripPillarTags()` removes any prior `pillar:*` entries and the selected pillar (if any) is re-added. This guarantees at most one pillar per item, with no parallel column or migration.",
      "Free re-use of existing UX: the pillar pill shows up in the Tags rail with its own count, can be clicked to filter the grid, is included in the card-level tag chip strip, and is indexed by the search box. The kind-pill filter and brand scoping continue to apply on top.",
      "Resilience: if a saved item references a pillar that has since been deactivated or renamed, the option is still rendered in the dropdown labelled `(inactive)` so editing the item doesn't silently drop the user's prior choice on save.",
    ],
  },
  {
    sortKey: "2026-05-16-e",
    date: "2026-05-16",
    category: "Feature",
    summary: "Added free-form tag categories to the Ideas section (Saved for Later, `/saved`). Each saved item can now carry up to 20 tags (e.g. `summer`, `visual`, `pillar:guides`, `competitor`, `winter`) entered as chips in the add/edit modal. A second-level Tags rail under the existing Kind pills lets you filter the grid by any tag in one click; tags also appear as small pills on every card and are included in the search index. Tags work across both brands (the column is brand-scoped, same as the rest of saved_items).",
    capabilities: [
      "Schema: added `tags jsonb NOT NULL DEFAULT '[]'` to `saved_items` (`lib/db/src/schema/savedItems.ts`), using the same `jsonb().$type<string[]>()` shape already used by `mediaAssets.tags`. Migration applied via `pnpm --filter @workspace/db run push`. The column is `NOT NULL DEFAULT []` so every pre-existing row reads back as an empty array — no backfill, no breaking change.",
      "API: `POST /api/saved-items` and `PATCH /api/saved-items/:id` (`artifacts/api-server/src/routes/savedItems.ts`) now accept a `tags` field via a new `cleanTags()` normaliser: trims each entry, lower-cases it, caps at 40 chars per tag and 20 tags per item, drops empties, and dedupes. The helper accepts both `string[]` and a comma-separated string for resilience. PATCH only writes `tags` if the key is present in the body, so partial updates that don't mention tags leave them alone.",
      "Frontend (`artifacts/virtu-ferries-brand-hub/src/pages/saved-items.tsx`): extended the `SavedItem` interface with `tags: string[]`, added a chip-style tag input to the `ItemModal` (Enter or comma commits, Backspace on an empty draft pops the last chip, X on each chip removes it). On submit, any half-typed draft is folded into the payload so users who hit Save without pressing Enter don't lose it. A `normalizeTag` helper on the client matches the server's normalisation so the UI never shows a chip that won't survive a round-trip.",
      "Frontend page: new `tagFilter` state + a Tags rail rendered between the Kind/search row and the grid. The rail lists tags from the currently kind-filtered subset, sorted by frequency (desc) then alphabetical, each pill showing its `#tag count`. Clicking a pill scopes the grid to that tag; clicking the active pill (or the dedicated Clear chip) removes the scope. The active tag is always shown even if it currently has 0 matches under the active Kind, so users can see why the grid is empty and one-click recover.",
      "Cards now show small accent-tinted `#tag` pills under the notes block, and the search box matches against tag text in addition to title/url/notes/host — so typing `summer` finds everything tagged `summer` even without picking the pill.",
    ],
  },
  {
    sortKey: "2026-05-16-d",
    date: "2026-05-16",
    category: "Bugfix",
    summary: "Fixed a regression introduced minutes earlier in 2026-05-16-c where uploading a thumbnail on a saved Idea would block form submission with the browser's native 'Please enter a URL' tooltip. The thumbnail `<input>` was declared as `type=\"url\"`, but the upload handler writes back the object-storage path (`/objects/...`) which fails HTML5 URL validation. Switched the field to `type=\"text\"` since it legitimately accepts both pasted external URLs *and* internal object paths — server-side validation in `routes/savedItems.ts` already trims/normalises whatever lands there.",
    capabilities: [
      "Changed the thumbnail input in `ItemModal` (`artifacts/virtu-ferries-brand-hub/src/pages/saved-items.tsx`) from `type=\"url\"` to `type=\"text\"`. The main URL field is left as `type=\"url\"` because it really is meant for external links.",
      "No backend changes needed — `cleanString` + the `!url && !title && !notes` guard in `POST /api/saved-items` continue to be the source of truth for what counts as a valid save.",
    ],
  },
  {
    sortKey: "2026-05-16-c",
    date: "2026-05-16",
    category: "UX",
    summary: "The Ideas section (Saved for Later, `/saved`) now supports inline editing of any saved item and image uploads for thumbnails — previously you could only add or delete, and the thumbnail was a URL-only field. A pencil button on every card opens the same modal used for adding, pre-filled with the item's current values, and PATCHes back to `/api/saved-items/:id` on save. The thumbnail field now offers a real file upload alongside the existing URL paste, with a live 96×96 preview that updates either way.",
    capabilities: [
      "Rewrote `artifacts/virtu-ferries-brand-hub/src/pages/saved-items.tsx` to merge the old add-only `AddItemModal` and the (newly added) edit flow into a single `ItemModal({ mode, existing, … })`. POST `/api/saved-items` fires when `mode === \"add\"`, PATCH `/api/saved-items/:id` fires when `mode === \"edit\"`. The parent page tracks `modal: { mode: \"add\" } | { mode: \"edit\"; item } | null` so only one modal can be open and the rename pre-fills every field including `kind` and `thumbnailUrl`. The grid's `onSaved` callback receives `(item, mode)` and either prepends (add) or replaces by id (edit) — no full reload.",
      "Added an `Upload image` button + hidden `<input type=\"file\" accept=\"image/*\">` to the thumbnail block. The handler hits the existing `POST /api/storage/uploads/request-url` presigned-URL endpoint, PUTs the file directly to object storage, and stores the returned `objectPath` in `thumbnailUrl` — the same mechanism already used by the Content Calendar's `NewPostModal` and `MediaLibrary`. Non-image MIME types are rejected up-front with a friendly error. A `Remove` link clears the thumbnail without deleting the underlying object. The URL paste field is preserved as a fallback for external thumbnails (e.g. CDN-hosted images) so nothing regresses.",
      "Added per-card `Edit` (pencil) action that opens the edit modal, sitting between the existing `Open ↗` link and the `Delete` button. Both edit and delete buttons use the same `opacity-0 group-hover:opacity-100` hover-reveal pattern so the card chrome stays clean at rest.",
      "Added a small `detectKindFromUrl` helper that, in add mode only, auto-picks the right kind from common URLs (YouTube/Vimeo/TikTok + Instagram Reels → `video`; Figma/Behance/Dribbble/Pinterest/Are.na → `design`; everything else http(s) → `link`). It bails as soon as the user manually clicks a kind chip (`kindTouched` flag) and never runs in edit mode, so editing an item never silently rewrites the kind the user originally chose.",
      "Added a local `resolveThumbnailSrc` helper that mirrors `components/MediaLibrary.tsx`'s `resolveSrc`: object-storage paths returned by `/api/storage/uploads/request-url` come back as `/objects/...`, which won't load if dropped straight into `<img src>`. The helper rewrites them to `${API}/api/storage/objects/...`, passes other root-relative paths through `${API}${path}`, and leaves external `http(s)` URLs untouched. Applied on both the card grid and the modal preview so uploaded thumbnails actually render.",
      "Made the per-card Edit and Delete buttons keyboard- and touch-accessible: they're now always visible on mobile (`opacity-100`) and only hover-revealed from `md` upwards, with `group-focus-within` + `focus-visible` rings so keyboard users can reach them. Both got proper `aria-label`s. The modal thumbnail preview now uses a state-based `thumbBroken` error fallback (reset via `useEffect` on `thumbnailUrl` change) instead of imperatively mutating `style.display`, so swapping in a working URL recovers the preview immediately.",
    ],
  },
  {
    sortKey: "2026-05-16-b",
    date: "2026-05-16",
    category: "UX",
    summary: "The Content Calendar 'Add post' modal now inherits the active channel filter as its default Market + Platform. If you have the IT-FB pill active and click Add post, the form opens with Market=Italian / Platform=Facebook already selected; same for EN-FB (Maltese / Facebook), IG (Maltese / Instagram, since Italian is FB-only), bare FB (Facebook, no market preset — Gozo Highspeed branch only), and Stories. With the All pill the form keeps its previous defaults (Maltese / Facebook).",
    capabilities: [
      "Extended `NewPostModal`'s props in `artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx` with two optional strings — `presetMarket` and `presetPlatform`. The form's initial-state computer (the non-edit branch of `useState<NewPostForm>(() => ...)`) now reads `presetMarket ?? \"Maltese Market\"` and `presetPlatform ?? \"Facebook\"` and uses the resolved platform to seed `format` via `formatsForPlatform(startPlatform)[0]`, so opening the modal on the IG pill seeds an Instagram-valid format instead of falling through to a Facebook one. Edit mode is untouched — `editPost` still wins, presets are only consulted when creating from scratch.",
      "Wired the parent `<NewPostModal>` invocation (next to the existing `presetDate` prop) to derive both presets from `marketFilter`: `it-fb` → Italian + Facebook, `en-fb` → Maltese + Facebook, `ig` → Maltese + Instagram, bare `fb` → Facebook only (no market — that branch is GHS-only), `story` → Story only, and `all` → both `undefined` (form keeps its existing Maltese + Facebook defaults). This means the day-click flow (which already passed `presetDate`) now also honours the channel filter, and the toolbar 'Add post' button does the same without any extra clicks.",
    ],
  },
  {
    sortKey: "2026-05-16-a",
    date: "2026-05-16",
    category: "UI",
    summary: "Redesigned the EN/IT Facebook channel filter pills in the Content Calendar toolbar. The 'flag as pill background' treatment shipped in 2026-05-15-p was visually heavy and made the FB platform signal compete with the country signal. Reverted to a clean outline Facebook glyph (matching the IG pill chrome) where the icon stroke itself is painted in the flag's colours via an SVG linear-gradient — Maltese FB shows the Malta bicolour and Italian FB shows the Italian tricolour, both as stripes along the glyph stroke.",
    capabilities: [
      "Added a new local `FlagFacebookIcon` component near the top of `artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx` (next to `platformIconList`). It renders the lucide Facebook glyph path (`M18 2h-3a5 5 0 0 0-5 5v3...`) inside a plain `<svg viewBox=\"0 0 24 24\" width=16 height=16>` with `fill=\"none\"` and `strokeWidth={2.2}` so it visually matches `<Facebook className=\"w-4 h-4\" strokeWidth={2.2}/>`. A `<defs><linearGradient id=\"fb-flag-{mt|it}\" x1=0 y1=0 x2=1 y2=0>` with hard-stop offsets at 50% (Malta: #D4D4D8 / #CF142B) and at 33.333% / 66.666% (Italy: #009246 / #D4D4D8 / #CD212A) paints the stripes; the path uses `stroke={`url(#${id})`}`. Pure white was nudged to neutral-300 (#D4D4D8) so the white half of the Maltese flag and the white centre of the Italian tricolour remain visible on top of the white pill chrome.",
      "Simplified the pill render block in the calendar toolbar (around lines 3755–3815). The `isFlag` branch no longer emits a flag-coloured background or the FB-blue icon override — both EN-FB and IT-FB now use the same neutral pill chrome as the IG pill (`text-[#71717A] hover:text-[#27272A]` when inactive, `bg-[#F4F4F5] ring-1 ring-[#18181B]/30` when active) and rely on the gradient-stroked `FlagFacebookIcon` to communicate market. Removed the `flagBg` constant, the `ring-2 ring-[#18181B]/70 ring-offset-1` active treatment, and the inactive `ring-1 ring-[#E4E4E7]` outline that the old flag-bg variant needed to keep its white stripe legible.",
    ],
  },
  {
    sortKey: "2026-05-15-q",
    date: "2026-05-15",
    category: "UX",
    summary: "Fixed a confusing 'I just saved a post but it doesn't show up in the calendar' bug — primarily seen on mobile when adding Italian-Market Facebook posts. After the strict FB/IG channel-filter change, leaving the EN-flag (Maltese FB) pill active and creating an Italian-Market FB post (or vice-versa) saved the post correctly but hid it from view because the active filter excluded that combination. The new-post modal now reports back the saved post's market/platform/cross_post/scheduled_date and the calendar auto-recovers visibility.",
    capabilities: [
      "Widened the `NewPostModal` `onSaved` callback signature in `artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx` from `() => void` to `(saved?: { market, platform, cross_post, scheduled_date }) => void`. The form's submit handler now passes these four fields up after a successful POST/PATCH so the parent page can make a defensive UX decision based on what was actually written. Existing callers that ignored the argument continue to work — the parameter is optional.",
      "In the parent `onSaved` handler (next to `setShowNewPost(false)` / `setLoadedMonth(null)`) added a re-check that mirrors the strict `visiblePosts` predicate: if the saved post wouldn't match the active `marketFilter` (e.g. saved an Italian FB post while EN-FB pill is active, or saved an FB post while IG pill is active, or saved a cross-post while a single-channel pill is active), `setMarketFilter('all')` so the user sees their work immediately. Also clears any stale `searchQuery` and jumps the calendar to the saved post's month/year if it differs from the currently viewed month, so a future-dated post doesn't silently land in 'next month' invisibly.",
    ],
  },
  {
    sortKey: "2026-05-15-p",
    date: "2026-05-15",
    category: "UX",
    summary: "Re-styled the EN/IT Facebook filter pills in the Content Calendar so the pill background paints the country flag itself — Maltese FB is the Malta bicolour (white | red, 50/50 vertical split) and Italian FB is the Italian tricolour (green | white | red, equal thirds). The Facebook icon sits on top in its brand blue (#1877F2) so it stays legible across all stripes.",
    capabilities: [
      "Replaced the previous solid `bg-[#1e82b4]` (en-fb) / `bg-[#e01814]` (it-fb) chip with a per-option `flagBg` constant in `artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx` — `linear-gradient(to right, #FFFFFF 50%, #CF142B 50%)` for Malta and `linear-gradient(to right, #009246 33.34%, #FFFFFF 33.34%, #FFFFFF 66.66%, #CD212A 66.66%)` for Italy. The flag bg is applied in BOTH the active and inactive states so the toolbar stays visually stable when the user toggles selection. Added `overflow-hidden` to the pill so the gradient is clipped to the rounded-full silhouette.",
      "Added a thin `ring-1 ring-[#E4E4E7]` hairline (with a darker `hover:ring-[#A1A1AA]`) on the inactive flag pills so the white stripe of each flag doesn't dissolve into the white pill container, and a chunkier `ring-2 ring-[#18181B]/70 ring-offset-1 ring-offset-[#FFFFFF]` on the active flag pill so the selected state remains obvious without overpainting the flag colours. Other filters (All / IG / FB / Story) keep their original solid-brand-colour active recipe and neutral grey inactive recipe — only en-fb and it-fb gained the flag treatment.",
    ],
  },
  {
    sortKey: "2026-05-15-o",
    date: "2026-05-15",
    category: "UX",
    summary: "Replaced the EN/IT circle-flag icons in the Content Calendar's channel filter pill with a Facebook icon tinted in each market's brand colour — Maltese (English) Facebook is now Virtu blue (#1e82b4) and Italian Facebook is Virtu red (#e01814). Instagram keeps its existing pink/orange gradient icon. The flags hinted at the market but didn't tell the user the platform was Facebook; the new tinted FB icons make both dimensions readable at a glance.",
    capabilities: [
      "Edited the Virtu branch of the channel filter pill in `artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx`. The `en-fb` and `it-fb` options now render `<Facebook className=\"w-4 h-4\" strokeWidth={2.2} />` instead of `<img>` flag SVGs from hatscripts.github.io. Their `title` was also tightened to 'Maltese (English) Facebook' and 'Italian Facebook' so the tooltip explicitly states both market AND platform.",
      "Introduced a per-option `inactive` tint string in the same map block so the EN/IT Facebook icons stay in their market colour even when the filter is not selected (`text-[#1e82b4]` for EN with a darker `#1a6fa0` hover, `text-[#e01814]` for IT with a darker `#b41310` hover). Other filters (All / IG / FB / Story) fall back to the neutral `text-[#71717A] hover:text-[#27272A]` recipe. The active state is unchanged: the button background paints the brand colour and the FB icon goes white via `currentColor`.",
    ],
  },
  {
    sortKey: "2026-05-15-n",
    date: "2026-05-15",
    category: "UX",
    summary: "Added a free-text search box to the Content Calendar toolbar so the team can find a post within the current month by name, caption snippet, visual direction, pillar, assignee, format or platform — useful when the calendar is dense and scrolling becomes slow.",
    capabilities: [
      "Added a `searchQuery` state hook in `artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx` and a rounded-pill search input rendered at the start of the right-hand toolbar group (next to the loading spinner and Share/Past/Posted buttons). The input shows a Lucide `Search` icon at the left, an inline `X` clear button when there's a value, and uses the same focus-ring tokens as the other hub controls (`#1e82b4`/30 ring + `/60` border) so it visually matches the existing channel filter pill.",
      "Wired the search into the existing `visiblePosts` predicate (just above the channel-filter checks) so it composes with every other filter — channel (FB/IG/EN-FB/IT-FB/All), past-day toggle, posted-collapse — and the sticky summary bar's Facebook/Instagram/format-breakdown chips automatically reflect the trimmed dataset (they already iterate over `visiblePosts`). The haystack lowercases title + caption + visual_direction + pillar + assigned_to + format + platform joined by `\\u0001` so a search like 'reel' matches by format and a search like a person's name matches by assignee, without any cross-field false positives.",
    ],
  },
  {
    sortKey: "2026-05-15-m",
    date: "2026-05-15",
    category: "UX",
    summary: "The Format dropdown on an Instagram post is now a strict whitelist of the four IG-supported formats — Reel - 9:16, Story, Carousel - 4:5, Single Image - 4:5. No Video, no Four Photo, no UGC on IG. Applies in the inline post detail edit and the new-post composer (Virtu and Gozo Highspeed flows).",
    capabilities: [
      "Added an `IG_FORMATS` constant alongside `FB_FORMATS` in `artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx` (`['Reel - 9:16', 'Story', 'Carousel - 4:5', 'Single Image - 4:5']`) and extended `formatsForPlatform(platform)` to branch on Instagram → `IG_FORMATS`, Facebook → `FB_FORMATS`, everything else (Both, Story, etc.) → generic `FORMATS`. All three Format `<select>` instances already call `formatsForPlatform(...)`, so no further wiring was needed — they pick up IG mode automatically when the user toggles platform to Instagram.",
      "Downstream matchers (best-time slot picker + sticky summary chip bucketer) continue to work unchanged because they prefix-match on `Reel`, `Carousel`, `Single Image`, etc., so suffixed IG values like `Carousel - 4:5` still bucket under the Carousel chip and route to the 13:00 best-time slot.",
    ],
  },
  {
    sortKey: "2026-05-15-l",
    date: "2026-05-15",
    category: "UX",
    summary: "Removed the standalone 'Single Image' option from the generic Format list (used by Instagram, Both and Story posts). The plain 'Single Image' value (no aspect ratio) is no longer offered anywhere in the composer — Facebook keeps its two suffixed variants ('Single Image - 4:5' and 'Single Image - 16:9') and IG/Both/Story now pick from Carousel, Reel, Video, Story, UGC, 4 Photos.",
    capabilities: [
      "Edited the `FORMATS` constant in `artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx` from `['Single Image', 'Carousel', 'Reel', 'Video', 'Story', 'UGC', '4 Photos']` down to `['Carousel', 'Reel', 'Video', 'Story', 'UGC', '4 Photos']`. The Facebook-specific `FB_FORMATS` whitelist is unchanged, so FB still offers Single Image - 4:5 and Single Image - 16:9 explicitly. Existing posts whose format was the legacy plain 'Single Image' value still render fine in the calendar grid (the dropdown shows whatever value is set even if it's not in the option list) — they just can't be re-selected without first switching platform to Facebook.",
      "No matcher changes were needed: the summary chip bucketer already accepts `fmt === 'single image'` alongside the suffixed variants via the `fmt === key || fmt.startsWith(key + ' -') || fmt.startsWith(key + ' ')` predicate, so legacy posts still bucket under the Single chip; and the best-time helper still prefix-matches `Single Image` for both FB and IG slots.",
    ],
  },
  {
    sortKey: "2026-05-15-k",
    date: "2026-05-15",
    category: "UX",
    summary: "The Format dropdown on a Facebook post is now a strict whitelist of the six FB-supported formats with explicit aspect ratios — Single Image · 4:5, Single Image · 16:9, Four Photo · 1:1, Video · 16:9, Reel · 9:16, UGC. Instagram, Both and Story keep the broader generic format list. Applies to the new-post composer (Virtu and Gozo Highspeed flows) and the inline Format edit on the post detail card.",
    capabilities: [
      "Added `FB_FORMATS` and a `formatsForPlatform(platform)` helper next to the existing `FORMATS` constant in `artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx`. The helper returns `FB_FORMATS` when `platform === 'Facebook'` and the original `FORMATS` array otherwise (so IG, Both and Story posts still see Single Image, Carousel, Reel, Video, Story, UGC, 4 Photos with no aspect-ratio suffix). Wired the helper into all three Format `<select>` instances: the inline `Editable` on the post detail card, the Virtu new-post composer's two-column Pillar+Format row, and the Gozo Highspeed compact composer's Pillar+Format row. The default `format` for a new post is now derived via `formatsForPlatform('Facebook')[0]` — i.e. `Single Image - 4:5` — since the form starts with platform=Facebook.",
      "Loosened the downstream string matchers that previously did exact `===` checks on the format value so they keep working with the new aspect-ratio-suffixed strings. The two 'Best time' suggestion blocks (Virtu's full composer and the Gozo Highspeed compact composer) now use `.startsWith('Reel')`, `.startsWith('Video')`, `.startsWith('Carousel')` and `.startsWith('Single Image')` instead of equality, so e.g. `Reel - 9:16` still routes to the 18:00 best-time slot. The sticky summary bar's per-format breakdown chips (Single, Carousel, Reels, Video, Stories, UGC, 4 Photos) now bucket variants together via `fmt === key || fmt.startsWith(key + ' -') || fmt.startsWith(key + ' ')`, with a special case so the `4 photos` chip also catches the new `four photo - 1:1` value. Story matching is unchanged.",
    ],
  },
  {
    sortKey: "2026-05-15-j",
    date: "2026-05-15",
    category: "UX",
    summary: "The Content Calendar's channel filter buttons (Facebook / Instagram / Maltese-FB / Italian-FB) are now strictly exclusive — a cross-post (platform 'Both', or legacy 'Facebook + cross_post=true') no longer appears in either single-channel view, only in 'All'. This makes the FB and IG views true separate calendars: when the user picks FB they see only FB-only posts, when they pick IG they see only IG-only posts, and 'All' shows everything including cross-posts.",
    capabilities: [
      "Tightened the `visiblePosts` predicate in `artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx` (the `useMemo`-style filter just below the `marketFilter` state). Previous behaviour: `ig = platform.includes('instagram') OR includes('both')` and `fb = platform.includes('facebook') OR includes('both')` — so a cross-post showed up in BOTH the FB and the IG single-channel filters, which contradicts the team's new workflow of treating Maltese FB and Maltese IG as separate calendars (a cross-post implicitly belongs to both, so it's not a clean fit for either single-channel view). New behaviour: `igOnly = platform === 'instagram'` and `fbOnly = platform === 'facebook' && !isCrossPost` where `isCrossPost = platform === 'both' || (platform === 'facebook' && cross_post === true)` — so cross-posts are visible *only* via the All filter, matching the user's literal request 'on FB i only see FB, on IG I only see IG, on All I see all'.",
      "All four channel buttons inherit the new strict semantics consistently: `fb` (single-brand view, e.g. Gozo Highspeed) returns `fbOnly`, `ig` returns `igOnly`, `en-fb` returns `fbOnly && !Italian Market` (= Maltese-FB-only), and `it-fb` returns `fbOnly && Italian Market`. The `story` filter is unchanged (Stories aren't cross-posted in either market).",
      "Re-pointed the sticky summary bar (Facebook / Instagram chips, per-format breakdown chips, total + unscheduled tally, and the bar's own visibility gate) from the unfiltered `posts` array to `visiblePosts`. Previously these counts always reflected the entire month dataset, which felt wrong when the user filtered down to FB-only or IG-only — the chips kept showing the cross-post-inclusive totals. They now mirror the calendar grid exactly.",
    ],
  },
  {
    sortKey: "2026-05-15-i",
    date: "2026-05-15",
    category: "UX",
    summary: "The Content Calendar duplicate picker now exposes the destination platform alongside the market, so the team can fan one post out into Maltese (EN) Facebook, Maltese (EN) Instagram, Maltese (EN) FB+IG cross-post, or Italian (IT) Facebook with one click — preparation for treating Maltese FB and Maltese IG as separate channels in the calendar.",
    capabilities: [
      "Expanded the duplicate popover in `CardDetailModal` (`artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx`) from two market entries to four destination presets: Maltese · Facebook, Maltese · Instagram, Maltese · FB + IG (cross-post), and Italian · Facebook. The current post's market+platform combination is still tagged 'same' so the user knows which preset matches the source. Italian is intentionally only listed once because the Italian channel is Facebook-only — picking any IG/Both/Story for the Italian preset is impossible by construction.",
      "Reworked `handleDuplicate` to accept `{ market, platform }` instead of just a market string. `cross_post` is now derived directly from the chosen platform (`platform === 'Both'`), removing the previous `cross_post = post.cross_post ?? false` carry-over which could have produced inconsistent state when changing platforms (e.g. duplicating an FB+IG cross-post into a single-channel Instagram entry would have kept `cross_post: true`). The Italian-Market guard still coerces IG/Both/Story → Facebook, but it now runs whenever the destination is Italian (regardless of source) since the user is making an explicit destination choice rather than carrying source state.",
      "The 'same' tag in the popover collapses the legacy dual-post representation (`platform: 'Facebook'` + `cross_post: true`) onto the 'Maltese · FB + IG' option, so source rows that were originally cross-posted via the legacy shape are correctly recognised by the picker rather than mis-tagging the plain Facebook entry as 'same'.",
    ],
  },
  {
    sortKey: "2026-05-15-h",
    date: "2026-05-15",
    category: "UX",
    summary: "The 'Duplicate' action on a Content Calendar post is now a small popover that lets the user pick the destination market (Maltese Market / Italian Market) before the copy is created — instead of always cloning into the same market. Cross-market duplicates also auto-fix the platform (Italian = Facebook only) and clear the pillar (the two markets use different pillar lists).",
    capabilities: [
      "Replaced the single 'Duplicate' button in `CardDetailModal` (`artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx`) with a tiny popover that lists both markets — 'Maltese Market (EN)' and 'Italian Market (IT)' — with the post's current market tagged as 'same'. Picking a market triggers `handleDuplicate(targetMarket)` directly; clicking outside the popover (transparent overlay) dismisses it. The same `Loader2` spinner state is shared with the new flow so the button still locks during the POST.",
      "Updated `handleDuplicate` to accept an optional target market and to derive a corrected payload only when the market actually changes: if the destination is 'Italian Market' and the source platform is Instagram / Both / Story, the duplicate is forced to 'Facebook' with `cross_post: false` — same coercion rule the new-post form applies in `set('market', ...)`. Same-market duplicates leave the platform / cross_post untouched so legacy posts are not silently rewritten. Pillar is preserved as-is across cross-market copies (the `content_posts.pillar` column is NOT NULL); the user simply re-picks the correct pillar in the modal afterwards. Workflow fields (status, creative_status, posted URLs) continue to reset on every duplicate as before.",
    ],
  },
  {
    sortKey: "2026-05-15-g",
    date: "2026-05-15",
    category: "Knowledge",
    summary: "Added a Malta Resources hub mirroring the existing Sicily Resources surface, and seeded its first reference list — Blue Flag Beaches across Malta and Gozo, sourced from Nature Trust Malta (the national Blue Flag operator at blueflagmalta.org). The new hub gives the team a Malta-side counterpart for destination research, primarily for 'Choose Malta' content on the Italy-facing channel and any seasonal Malta beach posts on the Maltese channels.",
    capabilities: [
      "Created `artifacts/virtu-ferries-brand-hub/src/pages/malta-resources.tsx` as a hub page that mirrors `sicily-resources.tsx` 1:1 — same `ResourceCard` shape, same hover+icon recipe, same brand-primary tint via `var(--brand-primary)`. It currently exposes a single card linking to Blue Flag Beaches; future Malta references (towns, events, transport) can be appended without changing the layout. Header copy frames Malta resources as Italy-facing 'Choose Malta' background plus seasonal Maltese-market support.",
      "Created `artifacts/virtu-ferries-brand-hub/src/pages/blue-flag-beaches-malta.tsx` modelled on the Sicily version, but with a `BeachRow { area, name, description, url }` shape instead of `{ area, beaches, drive }` because the Nature Trust listing publishes a per-beach narrative and a canonical detail URL (rather than a drive bracket from a single port). Uses the same `Tabs` component for a clean Malta / Gozo split — Malta tab carries 8 beaches (Mellieħa Bay, Golden Bay, Għajn Tuffieħa, Buġibba Perched Beach, Qawra Point, Fond Għadir, Med-Bar Reef Club, St George's Bay), Gozo tab carries 2 (Marsalforn Bay, Hondoq ir-Rummien). Each beach name is a real outbound link to its blueflagmalta.org detail page (target=_blank, rel='noopener noreferrer'). The header credits the source and links the master listing.",
      "Wired the new pages into `artifacts/virtu-ferries-brand-hub/src/App.tsx` (two new imports `MaltaResources` / `BlueFlagBeachesMalta`, two new `<Route>` entries at `/malta-resources` and `/blue-flag-beaches-malta`, both inside the existing `BrandedRoutes` switch so they sit under `BrandGuard`) and gated both 'Sicily Resources' and 'Malta Resources' sidebar entries in `SidebarLayout.tsx` behind `activeBrandSlug === 'virtu-ferries'` — these are Virtu-only destination references, so Gozo Highspeed users no longer see Sicily/Malta items in their nav (previously Sicily Resources was unconditionally visible). No existing routes were moved or removed.",
    ],
  },
  {
    sortKey: "2026-05-15-f",
    date: "2026-05-15",
    category: "UX",
    summary: "Added a per-format breakdown to the Content Calendar's sticky channel summary bar so the team can see at-a-glance how many Single Image, Carousel, Reels, Video, Stories, UGC, and 4 Photos posts are scheduled in the current month — alongside the existing Facebook / Instagram / Stories / posts-total chips. Each format chip only renders when at least one post in the visible month uses it, so the bar stays compact for sparse months and expands as the mix grows.",
    capabilities: [
      "Extended the post-count summary bar inside `artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx` (just under the month nav + market filter row) with a `FORMAT_META` registry — `[Single Image, Carousel, Reel, Video, Story, UGC, 4 Photos]` — that maps each format to a lucide icon (`ImageIcon`, `Layers`, `Film`, `VideoIcon`, `Circle`, `Users`, `Grid2x2`) and a brand-distinct tint (Virtu blue, sky-cyan, IG pink, violet, story purple, emerald green, amber). Iterates the registry, counts matching posts via case-insensitive `format` match (Story also pulls in posts where the platform tag mentions 'story', preserving the previous Stories chip behaviour), skips any format with a zero count, and renders the same chip recipe as the existing Facebook / Instagram blocks (small icon + 11px label + 12px bold tabular count). The right-aligned `posts total` and `unscheduled` indicator are unchanged. Imported `Layers`, `Users`, `Grid2x2`, and aliased `Video as VideoIcon` from `lucide-react` to back the new chips. The legacy standalone Stories block was folded into the same registry loop so the bar isn't duplicating the Stories count.",
    ],
  },
  {
    sortKey: "2026-05-15-e",
    date: "2026-05-15",
    category: "UX",
    summary: "Four targeted upgrades to the Content Calendar's post detail modal: (1) the date picker icon is now visible on the new light theme, (2) two new formats — 'UGC' and '4 Photos' — are selectable on every post, (3) a brand-new always-on inline-editable 'Graphic Text' textarea sits directly under Visual Direction so on-image / overlay copy lives next to the visual brief instead of being buried in the caption or notes, and (4) the underlying database schema gained a nullable `graphic_text` column on `content_posts` so the new field round-trips through GET, PATCH, POST, and the production data snapshot without any extra plumbing.",
    capabilities: [
      "Added a nullable `graphic_text: text('graphic_text')` column to `lib/db/src/schema/contentPosts.ts` and pushed it to the dev database via `pnpm --filter @workspace/db run push`. The column is intentionally nullable (not `.notNull().default('')` like `visual_direction`) so existing rows stay clean — an empty string would force every legacy post to render an empty 'Graphic Text' label, whereas `null` lets the inline-editable component show its placeholder instead. The `Editable.commit()` flow already converts trimmed-empty input back to `null`, so blanking the field clears it cleanly. The production snapshot bootstrap (`bootstrapFromSnapshot.ts`) automatically picks the new column up on the next prebuild dump because it dumps `content_posts` whole-row, so no `TABLES` array edit was needed; the prod DB will gain the column on the next deploy via the same drizzle push that already runs against prod.",
      "Wired `graphic_text` through the api-server PATCH route in `artifacts/api-server/src/routes/content.ts`: added it to the destructure list and to the conditional update spread as `...(graphic_text !== undefined && { graphic_text: graphic_text || null })`, mirroring the resources / visual_reference_url pattern so an empty string from the UI normalises back to `null` in the DB. Also added `graphic_text?: string` to the POST insert payload type at the top of the file; the existing `...p` insert spread carries it through, and the GET /content/posts list route uses `.select()` (whole row) so it auto-flows back to the client with no extra change. Verified the two narrow `.select({...})` projections at lines 346 (post history) and 1535 (plan generator) deliberately do not include `graphic_text` because neither surface displays it.",
      "On the frontend (`artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx`): added `graphic_text: string | null` to the local `ContentPost` type so TypeScript propagates the new field through every consumer, then dropped a new `<Editable label='Graphic Text' kind='textarea' value={post.graphic_text ?? null} placeholder='On-image / on-graphic copy (headline, overlay text, CTAs)…' onSave={v => patchPost({ graphic_text: v })} />` directly under the existing Visual Direction Editable inside `CardDetailModal`. It uses the same always-on inline-edit pattern shipped in 2026-05-15-d, so there's no separate save button — blur or Escape commits or reverts, and the per-label saving / saved indicator already in `Editable` works for free. `share-view.tsx` and `nico.tsx` were intentionally left alone: the public share page deliberately sanitises field-level data and the nico page uses a narrower projection — neither displays graphic text today.",
      "Extended the `FORMATS` array in `content-calendar.tsx` from `['Single Image','Carousel','Reel','Video','Story']` to `['Single Image','Carousel','Reel','Video','Story','UGC','4 Photos']`. Both the post detail modal's Format Editable (kind='select', options=FORMATS) and the New Post modal's Format dropdown (also reading FORMATS) pick up the two new options automatically. Existing rows with non-matching format strings keep displaying their stored value because the select renders the raw string above the dropdown — no migration required.",
      "Fixed the Content Calendar's date picker icon being nearly invisible on the light hub theme: replaced `[color-scheme:dark]` with `[color-scheme:light]` on all four input className strings in `content-calendar.tsx` — the inline-edit date input inside `Editable`, the `inputCls` shared by `NewPostModal`, the `compactInput` used by the in-modal date / time pickers, and the small checkbox helper. `color-scheme:dark` was telling the browser to render the native picker chrome (calendar icon, clear button, dropdown chevron) in dark mode — black icons on a now-white field — making them effectively unreadable. With `color-scheme:light` the browser paints the native picker UI in the light variant that matches the rest of the form. No JS or layout change needed; this is a CSS-only fix.",
    ],
  },
  {
    sortKey: "2026-05-15-d",
    date: "2026-05-15",
    category: "UX",
    summary: "Removed the secondary 'Edit post' workflow from the Content Calendar's post detail modal. Every field in the modal — title, platform, pillar, format, date, time, assignee, caption, visual direction, visual reference URL, link, and Drive folder — is now always-on inline-editable: no pencil button, no toggle into edit mode, no separate Edit Post Modal trip. Click any field, type, blur (or press Enter / ⌘+Enter for the caption), and the change PATCHes immediately with the same loading + green-check confirmation indicator next to each label.",
    capabilities: [
      "Refactored the shared `Editable` component in `artifacts/virtu-ferries-brand-hub/src/pages/content-calendar.tsx` to drop its `editing` state for the `text`, `url`, and `textarea` kinds. Inputs and textareas are now rendered live in every render with a quiet `border-[#E4E4E7] hover:border-[#A1A1AA]` resting style and the existing `focus:border-[#1e82b4]/60 focus:ring-2 focus:ring-[#1e82b4]/30` focus state, so the affordance is the field itself rather than a 'click to edit' button. Save fires on blur (or Enter for single-line, ⌘/Ctrl+Enter for textarea); Escape reverts to the last saved value and blurs. Removed the now-redundant Save / Cancel button row from the textarea variant — the `commit()` flow already debounces by comparing trimmed-to-null against the previous value, and the per-label saving / saved indicator stays put. The `select`, `date`, and `time` kinds were already always-live, so they were left untouched. The `linkify` and `displayClassName` props remain in the signature for backwards-compat but `linkify` now renders auto-linked URLs as a small caption underneath the textarea instead of as the (previously click-to-open) display surface.",
      "Replaced the title block at the top of `CardDetailModal` with the same always-on pattern: a single `<input>` with the bold heading typography, transparent border at rest, hover-revealed `border-[#E4E4E7]` rule, and the existing brand-blue focus border. Saves on blur via the existing `saveTitle` helper (Enter blurs, Escape reverts). Dropped the `editingTitle` / `setEditingTitle` state, the `startEditTitle` helper, and the secondary pencil icon button — all dead code now that nothing toggles the field in or out of edit mode.",
      "Removed the 'Edit post' button from the modal footer and dropped the `onEdit` prop from `CardDetailModal`'s signature. The parent no longer passes `onEdit`, so the legacy code path that closed the detail modal and re-opened the heavier edit-mode of `NewPostModal` is gone end-to-end. While cleaning up the orphaned wiring, removed the now-unused `editPost` state in `ContentCalendar` and the `<AnimatePresence>{editPost && <NewPostModal editPost={editPost}/>}` block — nothing was setting `editPost` anymore. `NewPostModal` itself stays put because the new-post flow (`showNewPost`) still uses it without the `editPost` prop. Duplicate, Download brief, and Delete remain in the modal footer because they're post-level actions, not field edits. Also removed the now-unused `Pencil` lucide import and dead `sc`/`Icon` locals at the top of `CardDetailModal`. Added a monotonic request token to `Editable.commit()` so a late PATCH response from a stale blur can't overwrite a newer in-flight value (last-response-wins race that the always-on blur model would otherwise expose). Verified by typecheck — only pre-existing unrelated errors remain.",
    ],
  },
  {
    sortKey: "2026-05-15-c",
    date: "2026-05-15",
    category: "Bugfix",
    summary: "Fixed a Content Calendar bug where Saturday weekly-schedule posts (and any other 'English Market' rows) were visible in the calendar list view but missing from the new-post date picker's mini-calendar dots. Root cause was a legacy market label — the old `English Market` value was renamed to `Maltese Market` across the hub a while ago, but production rows seeded from older snapshots still carried the legacy value. The new-post mini calendar filters posts by strict equality `p.market === form.market`, and since the dropdown only emits `Maltese Market` / `Italian Market`, every legacy `English Market` row silently dropped out — including the auto-generated Saturday `Weekly Schedule` posts on `Flexible / Operational`.",
    capabilities: [
      "Added `MARKET_RENAME_MAP` ({ 'English Market' → 'Maltese Market', 'English' → 'Maltese Market' }) and a paired `rewriteLegacyMarkets(client)` function in `artifacts/api-server/src/lib/bootstrapFromSnapshot.ts`, mirroring the existing `rewriteLegacyPillars` pattern. Wired it into the same `sp_legacy_rewrites` SAVEPOINT block at the end of `bootstrapFromSnapshot`, so on every production startup it runs `UPDATE content_posts SET market = 'Maltese Market' WHERE market IN ('English Market','English')`. Idempotent — once normalised, the next startup updates 0 rows and self-disables. The error label was widened from 'Legacy pillar rewrite failed' to 'Legacy pillar/market rewrite failed' so failures stay observable. Dev is unaffected because the bootstrap is gated on `NODE_ENV === 'production'` and the dev DB already only holds the canonical `Maltese Market` / `Italian Market` values.",
      "Verified via the production-DB read query that the bug surface was real before the fix: brand_id=1 had 43 rows with `market = 'English Market'` and 1 stray `market = 'English'`, including the Saturday weekly-schedule posts on 06.06, 13.06, 20.06, and 27.06. brand_id=2 had 62 such rows. After republish, all 105+ rows will be normalised to `Maltese Market` and the mini-picker dots will line up with the list view. No frontend changes needed — the calendar already renders dots for every `Maltese Market` row.",
    ],
  },
  {
    sortKey: "2026-05-15-b",
    date: "2026-05-15",
    category: "Strategy",
    summary: "Replaced the Virtu Ferries content pillar system again to match the latest approved 4-pillar set. The old 6-pillar list (Choose Virtu, Choose Sicily, Choose Malta, Virtu Recommends, The Crossing, The Community, Flexible / Operational) collapses into FOUR strategic pillars — The Virtu Experience (absorbs Choose Virtu + The Crossing into a single pillar with USP Mode and Experience Mode), Choose Sicily / Choose Malta (unchanged, still split by market), Virtu Recommends, and For the Feed (renamed from The Community) — plus Flexible / Operational which is now framed explicitly as a calendar tag, not a strategic pillar. Updated every surface that referenced the old vocabulary so the React UI, the AI agent prompt, the monthly-plan generator, the pillars DB table, and every existing content_posts row use the same names. Layout, theming, anchor-day cadence, and the 25-posts-per-month framework are preserved verbatim — only naming, definitions, and pillar references changed.",
    capabilities: [
      "Updated `lib/brand-knowledge/src/index.ts` in three places so the React UI and the agent prompt builder both pick up the new pillars without further sync: (a) `socialMedia.pillars` rewritten to the 4 strategic pillars + Flexible / Operational with the role + USP/Experience Mode breakdown for The Virtu Experience and the new participation rules for For the Feed; (b) `monthlyPlanning.pillarsEnglish` (Maltese-facing) with 'The Virtu Experience' as pillar 01 and 'For the Feed' as pillar 04; (c) `monthlyPlanning.pillarsItalian` with the same shape plus full Italian translations of every definition. Also updated `contentIdeas.themes` so the auto-generated content idea filters lead with 'The Virtu Experience' instead of the retired 'The Crossing'. The shared `formatBrandKnowledgeAsPrompt(slug)` helper consumes these arrays so the agent's system prompt automatically reflects the new pillars on the next request — no second source to update. Frontend pages (`/social-media`, `/monthly-planning`) re-render via `useBrandContent` with no schema changes (`PillarOverview` already supports the longer titles).",
      "Rewrote `artifacts/api-server/src/lib/brandGuidelines.ts` CONTENT PILLARS section in full to reflect the new model: a market-rule preamble that explicitly notes The Virtu Experience replaces and absorbs the old Choose Virtu + The Crossing pillars, and For the Feed replaces The Community. The Virtu Experience definition now spells out the two content modes — USP Mode (speed, comfort, car flexibility, pet travel, Starlink WiFi, luggage freedom, daily/early crossings, no airport friction, booking CTAs, offers) and Experience Mode (onboard moments, crew, passengers, drone shots, sea views, ferry visuals, the feeling of travelling by sea) — with the rule that every week needs at least one of each mode. Virtu Recommends gained the explicit framing that Pozzallo is a strategic STARTING POINT for Sicily, not just a port. For the Feed gained the participation guardrails (not filler, trends must serve the strategy, avoid naming specific trend examples, never sell). Flexible / Operational is now framed as a calendar tag, not a strategic pillar. The Offers placement note is updated to route promotional content into The Virtu Experience / Virtu Recommends / Flexible / Operational. MONTHLY FRAMEWORK pillar targets and anchor-day grid rewritten (Mon = Virtu Experience USP Mode, Sun = Virtu Experience Experience Mode, Fri = For the Feed, Sat = Virtu Recommends + Flexible / Operational schedule, Tue/Wed/Thu = Choose Sicily/Malta rotation). HARD RULES line 6 ('Link in bio to book') updated to reference 'The Virtu Experience in USP Mode' and to forbid the same CTA on Experience-Mode and For the Feed posts. API server restarted so the live system prompt reflects the new pillars immediately.",
      "Updated `artifacts/api-server/src/routes/content.ts` so the AI monthly-plan generator emits the new pillar names: (a) `DEFAULT_PILLARS` collapsed from 7 to 6 rows (The Virtu Experience/both, Choose Sicily/english, Choose Malta/italian, Virtu Recommends/both, For the Feed/both, Flexible / Operational/both — used by the `/api/content/pillars` GET fallback when the table is empty); (b) the English-market and Italian-market pillar blocks inside `buildMonthlyPlanPrompt` rewritten with the new pillars, the explicit USP / Experience Mode breakdown for The Virtu Experience, the Pozzallo-as-starting-point framing for Virtu Recommends, and the participation rules for For the Feed; (c) the PILLAR TARGETS section under POSTING STRUCTURE rewritten to the new mapping (The Virtu Experience ≈ 9 posts with ≥3 USP-Mode offer slots and the rest Experience Mode, Choose Sicily/Malta ≈ 9, Virtu Recommends ≈ 5, For the Feed ≈ 3, Flexible / Operational reserved for the Saturday schedule + disruption only). The Saturday weekly-schedule HARD RULE (`pillar: \"Flexible / Operational\"`) is unchanged. The 25-posts-per-month total, format targets, and pinned-idea rules are unchanged.",
      "Migrated the live database in a single transaction: TRUNCATEd the 7 old `pillars` rows for `brand_id=1` and re-inserted the 6 new rows in the canonical sort_order (The Virtu Experience/both/0, Choose Sicily/english/1, Choose Malta/italian/2, Virtu Recommends/both/3, For the Feed/both/4, Flexible / Operational/both/5), then UPDATEd every existing `content_posts` row for `brand_id=1` with the deterministic mapping — `Choose Virtu → The Virtu Experience` (32 rows), `The Crossing → The Virtu Experience` (0 rows existed but the rule still runs in case future seeds reintroduce it), `The Community → For the Feed` (9 rows). Verified post-migration: every brand_id=1 post now uses one of the 5 approved labels (Choose Malta 5, Choose Sicily 15, For the Feed 9, The Virtu Experience 32, Virtu Recommends 1); no orphan values remain. Cosmetic notes referencing old pillar names in `lib/db/src/seeds/events.ts` were updated for consistency so future seed runs don't reintroduce stale names. Frontend default new-post pillar in `content-calendar.tsx` was flipped from 'Choose Virtu' to 'The Virtu Experience' and the example text in `settings-pillars.tsx` was extended to mention the new strategic pillars on Both. The `data-snapshot.json` bundled with the api-server build was regenerated from the migrated dev DB so a production republish carries the new vocabulary forward via the existing `bootstrapFromSnapshot` flow. No layout, theming, dropdown, or component changes — purely a naming and definition swap.",
    ],
  },
  {
    sortKey: "2026-05-15-a",
    date: "2026-05-15",
    category: "Feature",
    summary: "Added a live Sicily events feed to the Virtu Ferries hub, mirroring the existing Gozo Highspeed → eventsingozo.com feed but for the destination side of the Malta ↔ Pozzallo route. The Events page now ends with a read-only `visitsicily.info` section showing every event scraped hourly from the official Visit Sicily portal — title, date range, location, description, categories, plus a direct link to the source page and (where listed) the official event website. The feed is brand-gated to Virtu Ferries only and is also injected into the Brand Agent's system prompt as a `# SICILY LIVE EVENTS` block, so the agent can reference real, current Sicilian events when proposing 'Choose Sicily' or 'Virtu Recommends' content without inventing them.",
    capabilities: [
      "Built a server-side scraper at `artifacts/api-server/src/routes/sicilyEvents.ts` that walks 5 RSS pages of `visitsicily.info/en/category/categorie-en/evento-en/feed/` in parallel (~50 events), dedupes by URL, then fetches each event's detail page with bounded concurrency 6 and a 12s per-request timeout. The detail-page parser flattens the HTML and locates the structured footer block visitsicily emits (`LOCATION / START dd/mm/yyyy hh:mm / END / WEBSITE / E-MAIL / SOCIAL / PLACES / CATEGORIES / AROUND`) by walking labels in order, so each event yields typed `start`, `end`, `location`, `website`, `social`, and `categories[]`. The body description is sliced from the universal Avada-theme `Detail … Share this content!` markers and capped at 1200 chars to protect the agent prompt budget. Image is taken from `og:image`. Exposed `GET /api/sicily-events` (returns `{source, fetchedAt, cached, count, events[]}`) and `getSicilyEvents(force?)` for in-process consumers; results are cached for 1h with an inflight-promise guard so the first request after expiry doesn't fan out concurrent crawls. Route is brand-gated — non-virtu-ferries brands get a 404 — and supports `?refresh=1` to bust the cache. Registered the router in `artifacts/api-server/src/routes/index.ts`.",
      "Added a `SicilyEventsSection` component to `artifacts/virtu-ferries-brand-hub/src/pages/events.tsx` that renders below the existing manual events list, gated to `activeBrand.slug === 'virtu-ferries'` (the Gozo feed stays gated to GHS — both can never be visible at once). UI mirrors the Gozo feed's hub-chrome light theme: a `Live feed` eyebrow, the `visitsicily.info` heading, an attribution + photo-rights paragraph, last-fetched timestamp, and a `Refresh` button that POSTs to `/api/sicily-events?refresh=1`. Each event renders as a `SicilyFeedCard` with the brand-blue accent stripe, mini date stamp, formatted en-GB date range, MapPin location, expandable description with Read more / Show less, and lucide `Globe`/`ExternalLink` actions linking to the official event website and the visitsicily source page respectively. Events are grouped into month rails via `groupSicilyByMonth` reusing the existing `MonthRail` primitive, and 'Date TBA' entries fall to the bottom. The new component uses the same `useState`/`useCallback`/`useEffect` plumbing already imported in events.tsx — no new dependencies.",
      "Wired the same feed into the Brand Agent's system prompt at `artifacts/api-server/src/routes/chat.ts`. `buildKnowledgeContext` now takes `brandSlug` alongside `brandId`; when the active brand is `virtu-ferries`, after the existing `# UPCOMING EVENTS (from the events database)` block, the builder calls `getSicilyEvents()` (which hits the same 1h cache the route uses, so this adds zero latency on hot turns), filters to the next 25 events whose start date is today or later, and emits a `# SICILY LIVE EVENTS (from visitsicily.info, refreshed hourly)` block listing each as `[YYYY-MM-DD → end] Title · Place [Cat1, Cat2] — sourceUrl`. The block carries explicit usage guidance in its preamble — credit visitsicily.info, treat their photos as visual reference only — so the agent reproduces the same attribution discipline the UI shows the user. Soft-fails (logs and continues) if the external feed is unavailable so a slow visitsicily response can never block a chat turn. `brandGuidelines.ts` (line 37) was updated in the same change to reference the new SICILY LIVE EVENTS block alongside the events database, so the KNOWLEDGE BASE PROTOCOL acknowledges it as authoritative. API server restarted so the live system prompt picks the change up immediately.",
    ],
  },
  {
    sortKey: "2026-05-12-aa",
    date: "2026-05-12",
    category: "Strategy",
    summary: "Replaced the Virtu Ferries content pillar system across every surface — definitions, AI agent prompt, monthly planning targets, the pillars DB table, and every existing content_posts row — with the newly approved 7-pillar set: Choose Virtu, Choose Sicily (Maltese-facing), Choose Malta (Italy-facing), Virtu Recommends, The Crossing, The Community, and Flexible / Operational. Retired the old names (Why VF, Why Sicily, Why Malta, VF Experience, VF Recommends, For the Feed, Sicily/Malta Experience, VF Recommends Malta) so every content card, dropdown, and prompt now uses the same vocabulary the brand actually uses. Layout, styling, anchor-day cadence, and the 25-posts-per-month framework are preserved verbatim — only naming, definitions, and pillar references changed.",
    capabilities: [
      "Updated `lib/brand-knowledge/src/index.ts` in three places so the React UI and the agent prompt builder both pick up the new pillars without further sync: (a) `socialMedia.pillars` (5 pillars + Flexible/Operational with the role + definition copy from the approved spec); (b) `monthlyPlanning.pillarsEnglish` with 'Choose Sicily' for the Maltese-facing channels; (c) `monthlyPlanning.pillarsItalian` with 'Choose Malta' for the Italy-facing channel and Italian translations of every definition. The shared `formatBrandKnowledgeAsPrompt(slug)` helper consumes these arrays so the agent's system prompt automatically reflects the new pillars on the next request — no second source to update. Frontend pages (`/social-media`, `/monthly-planning`) re-render via `useBrandContent` with no schema changes (`PillarOverview` already supports the longer titles).",
      "Rewrote `artifacts/api-server/src/lib/brandGuidelines.ts` CONTENT PILLARS section in full: added a market-rule preamble explaining the Choose Sicily / Choose Malta split (Facebook Malta + Instagram = Choose Sicily, Facebook Italy = Choose Malta), then replaced the five-pillar definitions with six (Choose Virtu, Choose Sicily/Choose Malta, Virtu Recommends, The Crossing, The Community, Flexible / Operational), each with role + goal + what-it-includes + tone + rule + anchor day. Explicit 'Do NOT create an Offers pillar' note — offers now sit under Choose Virtu, Virtu Recommends, or Flexible / Operational. MONTHLY FRAMEWORK pillar targets and anchor-day grid rewritten to match (Mon=Choose Virtu, Fri=The Community, Sun=The Crossing, Sat=Virtu Recommends + Flexible/Operational schedule, Tue/Wed/Thu = Choose Sicily/Malta rotation). CONTENT GENERATION RULES line 6 ('Link in bio to book') and HARD RULES line 5 ('Never include a booking CTA in a For the Feed post') updated to reference 'Choose Virtu' and 'Community'. API server restarted so the live system prompt reflects the new pillars immediately.",
      "Updated `artifacts/api-server/src/routes/content.ts` so the AI monthly-plan generator emits the new pillar names: (a) `DEFAULT_PILLARS` rewritten to the 7-pillar list (Choose Virtu/both, Choose Sicily/english, Choose Malta/italian, Virtu Recommends/both, The Crossing/both, The Community/both, Flexible / Operational/both — used by the `/api/content/pillars` GET fallback when the table is empty); (b) the English-market and Italian-market pillar blocks inside `buildMonthlyPlanPrompt` rewritten with the new names + one-line definitions; (c) the Saturday weekly-schedule HARD RULE flipped from `pillar: \"Why VF\"` to `pillar: \"Flexible / Operational\"` (per the spec rule that Flexible / Operational is reserved for required schedules and service info, never editorial); (d) the PILLAR TARGETS section under POSTING STRUCTURE rewritten to the new mapping (Choose Virtu = 3 offer posts, Choose Sicily/Malta ≈ 9, Virtu Recommends ≈ 5, The Crossing ≈ 4, The Community ≈ 3, Flexible / Operational reserved for the Saturday schedule + disruption only). The 25-posts-per-month total, format targets, and pinned-idea rules are unchanged.",
      "Migrated the live database in a single transaction: TRUNCATEd `pillars` for `brand_id=1` and re-inserted the 7 new rows in the canonical sort_order, then UPDATEd every existing `content_posts` row for `brand_id=1` with the deterministic mapping — `Why VF → Choose Virtu` (32 rows), `Why Sicily` Maltese Market `→ Choose Sicily` (13 rows), `Why Sicily` Italian Market `→ Choose Malta` (1 row, per the market rule that Italy-facing destination content uses Choose Malta), `Why Malta → Choose Malta` (4 rows), `VF Recommends` / `VF Recommends Malta → Virtu Recommends` (1 row), `VF Experience` / `Virtu Ferries Experience → The Crossing` (0 rows existed), `For the Feed → The Community` (9 rows). Verified post-migration: every brand_id=1 post now uses one of the 7 approved pillar names; no orphan values remain. The Saturday weekly-schedule generator will start placing future schedule posts under `Flexible / Operational`. Cosmetic notes referencing old pillar names in `lib/db/src/seeds/events.ts` (5 events) were also updated for consistency so future seed runs don't reintroduce stale names. Frontend default new-post pillar in `content-calendar.tsx` and the example text in `settings-pillars.tsx` were updated to 'Choose Virtu' / 'Choose Sicily' / 'Choose Malta'. No layout, theming, dropdown, or component changes — purely a naming and definition swap.",
    ],
  },
  {
    sortKey: "2026-05-12-z",
    date: "2026-05-12",
    category: "Feature",
    summary: "Three small but high-impact polish changes to the Content Calendar so the day-to-day editing flow stays focused on upcoming work and the in-place caption editor reaches feature parity with the modal: (a) past days are now hidden by default — the calendar opens straight onto today + upcoming so the user always sees the next thing to ship instead of scrolling past archived rows; (b) the inline `<Editable>` Caption field in the post-detail panel now has the same `Bold selection` action that already exists on the Edit / Generate modal caption (Unicode bold that survives Facebook & Instagram paste); (c) the modal caption textarea is now a uniform 2 rows for both brands instead of 5 rows on Virtu / 2 on GHS, so the editor stays compact and the form fits on a laptop screen without scrolling.",
    capabilities: [
      "Past-day rows in `CalendarGrid` (`pages/content-calendar.tsx`) now return `null` whenever `past && !showPast`, instead of rendering the previous thin one-line collapsed row. This means the visible day list starts at today and runs forward — past content is fully out of the way unless the user explicitly clicks the existing toolbar `View past` toggle, which still expands every past day in the month back to its full row exactly as before. No data is ever filtered out server-side; this is a purely UI presentation toggle, so all past posts remain queryable, exportable to PDF, and reachable from share links and the Skipped Posts page. Removed the now-unreachable thin-row JSX block (~40 lines) so the grid renderer has only one rendering path and is easier to reason about.",
      "Extended the inline `Editable` field component with a new optional `withBoldButton?: boolean` prop. When set on a `kind=\"textarea\"` field, the label header gains a right-aligned `Bold selection` button (with the existing `Bold` lucide icon) that, while editing, calls the existing `applyBoldToTextarea` helper against the field's own `textareaRef` — the same Unicode-bold transform (`isBoldUnicode` / `toBoldUnicode` / `fromBoldUnicode`) used by the Edit / Generate modal caption, so a selection that is already bold gets toggled back to plain. `onMouseDown={e => e.preventDefault()}` keeps the textarea focused so the selection survives the click and the cursor doesn't blur-commit prematurely. Wired this prop on the post-detail panel's `<Editable label=\"Caption\">` so the same affordance is available in the inline editor that was previously modal-only — the affordance is hidden on every other Editable instance (visual direction, resources, etc.) since they don't carry social-media copy.",
      "Modal caption textarea (`NewPostForm` / Edit-post body) reduced from `rows={isVirtu ? 5 : 2}` to a flat `rows={2}` for both Virtu Ferries and Gozo Highspeed. Previously Virtu had a 5-row caption box dating from when the modal also held the AI-suggestion preview inside it; the suggestion preview now lives outside, so the extra height was just empty space pushing other fields below the fold. Going to 2 rows matches the GHS sizing the user already preferred and the textarea still grows as the user types because the parent `inputCls` keeps `resize-none` only on overflow — content is never clipped, just no longer takes 3 extra rows of vertical space when empty. No schema, route, or data changes.",
    ],
  },
  {
    sortKey: "2026-05-12-y",
    date: "2026-05-12",
    category: "Design",
    summary: "Inverted the Content Calendar (page shell + day grid + Edit-post / Generate / Import modals — the entire `content-calendar.tsx`, all 4068 lines) to the soft off-white hub-chrome recipe so it matches the rest of the hub instead of being the last dark island. Originally kept dark in 2026-05-12-t as a 'deliberate dark workspace', but with every other page now on the light recipe it read as inconsistent rather than intentional, so it joins the light system. Brand blue / amber / red, status pills, and platform colours (Facebook / Instagram / TikTok / etc.) are all preserved verbatim.",
    capabilities: [
      "Sentinel-based hex remap on `pages/content-calendar.tsx` (206 lines changed) to convert the dark hub-chrome recipe to its light counterpart token-for-token without colliding with reused hex codes (e.g. `#E4E4E7` was previously used as text-body on dark and is now used as a border on light — the sentinel pass routes them correctly): `#0E0E0E → #F5F5F5` (canvas), `#121212 / #161616 / #1C1C1C → #FFFFFF` (panel / surface / input bg), `#181818 / #1A1A1A → #FAFAFA` (alt surface / subtle), `#222222 / #272727 / #2D2D2D → #E4E4E7` (divider / borders), `#333333 → #D4D4D8`, `#454545 → #A1A1AA`, `#FAFAFA → #18181B` (text primary), `#E4E4E7 → #27272A` (text body), `#A1A1AA / #8E8E96 → #71717A` (text muted), `#6B6B73 → #A1A1AA` (text micro). The lone `#0F1A22` selected-state pill (deep dark blue paired with brand blue ring) became `bg-[#1e82b4]/10` so the selected state stays visibly distinct on the light card without going neon. Same Tailwind→hex remap as 2026-05-12-w (`bg-gray-50 → bg-[#F5F5F5]` etc.) was reapplied to the Import-CSV modal section that already used Tailwind grays so it shares one vocabulary with the rest.",
      "Deliberately not touched: brand colours (`#1e82b4`/`#1a6d99`/`#1a6fa0`/`#1666a0`/`#5BB6E0` Virtu blues, `#f6a610`/`#FBC764` amber, `#e01814` red, `#1d3289` GHS), platform/social colours (`#1877F2` Facebook, `#E1306C` Instagram, `#A855F7`/`#8B5CF6`/`#7b3ff2` TikTok-family, `#F59E0B`), status-pill background colours (`bg-emerald-500`, `bg-sky-500`, `bg-slate-700`, `bg-red-500/90`), and the modal backdrop convention (`bg-black/30..80 backdrop-blur`). Updated replit.md theming-model implementation note to drop 'Content Calendar shell + modals stay dark' from the layer-1 dark recipe and remove the corresponding 'dark workspace' line — the only deliberately-dark surfaces remaining are the SidebarLayout sidebar inner, the BrandAgent chat, and the brand-picker entry hero.",
      "Caught and fixed three classes of light-mode regressions from the bulk hex remap that the architect surfaced after the initial pass: (a) **invisible white hovers on white surfaces** — 17 instances of `hover:bg-[#FFFFFF]`/`hover:bg-[#FAFAFA]` (mini-calendar nav, day-cell hovers, top-toolbar icon buttons, modal close buttons, list-row hovers) all rebuilt to `hover:bg-[#F4F4F5]` so the hover state is actually visible against the white card; (b) **vanishing borders/dividers** — 1 `divide-[#FAFAFA]`, 2 `border-[#FAFAFA]`, and 1 `h-px bg-[#FAFAFA]` separator promoted to `#E4E4E7` so structural boundaries between rows and sections actually render; (c) **low-contrast pill text** — 17 status / event / approval pills using `text-{emerald,sky,red,purple,amber,blue}-300` (designed for /10 brand tints on dark bg) bumped to `-700` so the chip text is legible on light brand-tint backgrounds. Also tightened two over-translucent header strips (`bg-[#FFFFFF]/90 → /95`, `bg-[#FFFFFF]/60 → bg-[#FAFAFA]`) so they read as distinct layers rather than fading into the canvas. Root cause of the misses on the first pass: the original word-boundary regex `\\bhover:bg-\\[#FFFFFF\\]\\b` doesn't match because `]` is not a word character — switched to literal string replacement to catch them all. No data, schema, or API changes.",
    ],
  },
  {
    sortKey: "2026-05-12-x",
    date: "2026-05-12",
    category: "Feature",
    summary: "Added a `Reschedule` action to every row on the Skipped Posts page so a put-aside post can be sent back to the calendar on a brand-new date in one step — previously the only options were `Restore` (which kept the original `scheduled_date`, often landing the post in the past) or `Delete`. Now there's a per-row date picker that updates `scheduled_date` and flips status from `skipped` back to `pending` in a single PATCH so the post leaves the archive and reappears on the chosen day in the Content Calendar.",
    capabilities: [
      "Wired a new `RescheduleBtn` inline on each row of `pages/skipped-posts.tsx` (sits to the left of the existing `Restore` and `Delete` actions). Clicking it swaps the action cell for a compact inline form: native `<input type=\"date\">` (autofocused, defaulting to the post's existing `scheduled_date` or today's `YYYY-MM-DD` derived from local-time getters so there's no UTC off-by-one), a green check confirm, and a muted X cancel. Confirm calls a new `reschedule(id, newDate)` handler that PATCHes `/api/content/posts/{id}` with `{ scheduled_date: newDate, month: newDate.slice(0, 7), status: 'pending' }` and optimistically removes the row from the skipped list so the table updates instantly. On error the handler refetches the full skipped list via `load()` instead of restoring a stale local snapshot — concurrency-safe so two near-simultaneous row actions can't resurrect each other's removed rows.",
      "Server side: extended the existing `PATCH /api/content/posts/:id` route in `artifacts/api-server/src/routes/content.ts` to also accept the `month` field (validated as `/^\\d{4}-\\d{2}$/`, returns 400 on bad format) and pass it into the Drizzle `.update()` set clause. This was required because `content_posts.month` is `notNull` and the calendar's `GET /content/posts?month=YYYY-MM` filter buckets rows by it — without sending `month` alongside `scheduled_date`, rescheduling a post into a different month would have left it stuck in the old month bucket and not appearing on the chosen calendar day. The PATCH stays scoped to `req.brandId` so brand isolation is preserved. Tightened the existing `Restore` button's tooltip to `Restore as draft (keeps current date)` so the two actions are clearly distinct: Restore = move back to drafts on the original date; Reschedule = move back to drafts on a new date. Visual recipe matches the page's hub-chrome light tokens (`bg-[#F5F5F5]` form chrome, `border-[#E4E4E7]`, `text-[#18181B]`, `#39A15F` accent for confirm).",
    ],
  },
  {
    sortKey: "2026-05-12-w",
    date: "2026-05-12",
    category: "Design",
    summary: "Aligned the remaining 17 per-brand Virtu Ferries pages to the same hub-chrome light recipe (`#F5F5F5` page bg, `#FFFFFF` cards, `#E4E4E7` borders, `#18181B/#27272A/#71717A/#A1A1AA` text) so the entire hub now sits on one consistent surface system below the dark sidebar — previously each per-brand page was using the close-but-not-identical Tailwind `gray` scale, which read slightly bluer than the zinc-flavoured recipe used everywhere else.",
    capabilities: [
      "Token-remapped 17 per-brand pages + the SidebarLayout outer wrapper (367 lines): assets, brand-history, brand-identity, customer-promise, events, excursions, hop-on-hop-off, media-library, offers, onboard-experience, resources, saved-items, schedule-fares, sicily-towns, skipped-posts, travel-info, unique-selling-points. Word-boundary regex remap converted the Tailwind gray scale to the recipe hexes so future edits use the same vocabulary as the inverted hub-chrome pages: `bg-gray-50 → bg-[#F5F5F5]`, `bg-gray-100 → bg-[#F4F4F5]`, `border-gray-100/200/300 → border-[#F4F4F5]/[#E4E4E7]/[#D4D4D8]`, `divide-gray-100/200 → divide-[#F4F4F5]/[#E4E4E7]`, `ring-gray-200/300 → ring-[#E4E4E7]/[#D4D4D8]`, and the full text scale `text-gray-{900,800,700,600,500,400} → text-[#18181B]/[#27272A]/[#3F3F46]/[#52525B]/[#71717A]/[#A1A1AA]`. All hover/placeholder variants (`hover:bg-gray-50`, `placeholder:text-gray-400`, etc.) were caught by the same word-boundary patterns. SidebarLayout outer wrapper moved from `bg-gray-50` to `bg-[#F5F5F5]` so the canvas behind every page is exactly the recipe page-bg token.",
      "Deliberately did NOT touch: (a) literal `bg-white` (already `#FFFFFF`, no normalization gain); (b) brand-coloured tints like `bg-blue-50`, `bg-amber-50`, `bg-[#1e82b4]/5`, gradient stops `from-blue-50/30 via-white to-amber-50/20` and the dual-radial Hero treatments on `offers`/`schedule-fares`/`travel-info`/`brand-history` because those tints are intentional brand-flavour signals, not surface tokens; (c) hardcoded brand hex literals (`#1e82b4` Virtu blue, `#f6a610` amber, `#e01814` alert red, GHS `#1d3289`/`#ea2d3f`, platform colours `#1877F2`/`#E1306C` for Facebook / Instagram on `skipped-posts`); (d) the `index.css` `.surface-card` helper class (still `bg-white border-gray-100/80`) — left untouched because changing it would silently affect any future component that opts in. Visible delta is small (Tailwind `gray-50` and recipe `#F5F5F5` differ by ~5 RGB units), but the recipe is now consistent token-for-token across the entire hub. No data, schema or API changes.",
    ],
  },
  {
    sortKey: "2026-05-12-v",
    date: "2026-05-12",
    category: "Design",
    summary: "Inverted the hub-chrome page bodies to a soft off-white so the dark sidebar reads as a distinct vertical surface against the working canvas — sidebar stays black, BrandAgent chat stays dark (continuity with the sidebar), Content Calendar shell + modals stay dark (the deliberate dark workspace built earlier this session), brand picker entry hero stays dark (no sidebar to contrast against), and every other page body flipped to the standard light recipe used by the per-brand pages.",
    capabilities: [
      "Inverted 8 hub-chrome page files (269 lines): home / dashboard, settings, settings-pillars, knowledge-base, changelog, scraper, nico, and the social-media sticky channel bar. Surface tokens flipped via sentinel-based replacement: page bg `#0E0E0E/#121212 → #F5F5F5`, surface / card `#161616/#1C1C1C → #FFFFFF`, alt surface `#181818/#1A1A1A → #FAFAFA`, divider / hover bg `#222222 → #F4F4F5`. Border tokens flipped: `#272727/#2D2D2D → #E4E4E7`, hover-quiet `#333333 → #D4D4D8`, hover-strong `#454545 → #A1A1AA`. Text tokens inverted: primary `#FAFAFA → #18181B`, body `#E4E4E7 → #27272A`, small muted `#8E8E96 → #71717A`, micro `#6B6B73 → #A1A1AA`. The medium muted `#A1A1AA` was deliberately not in the source list because it works on both themes (now serves as the micro tone on light surfaces). Brand accents (`#1e82b4`, `#39A15F`) and alpha tints kept untouched so the same primary-action treatment carries across the theme split.",
      "Kept dark, with rationale, four surfaces that the user implicitly singled out: (a) `SidebarLayout` sidebar inner stays `bg-[#121212]` because the user explicitly said 'I like that sidebar is black'; (b) `BrandAgent` chat panel stays dark to share the sidebar's tone, since it's an embedded chat overlay inside `/dashboard` and reads as continuation of the sidebar rather than a content surface; (c) `content-calendar.tsx` (calendar shell + CardDetailModal + NewPostModal) stays dark because the user spent this same session approving the dark conversion of those exact surfaces; (d) `brand-picker.tsx` (`/`) stays dark because it's a standalone entry hero with no sidebar to contrast against — the dramatic dark canvas is the point. After the bulk inversion an architect review caught six leftover dark fragments inside the now-light pages and they were patched in the same entry: nico post-card hover border was a no-op (`hover:border-[#E4E4E7]` on a `border-[#E4E4E7]` card → repointed to `#D4D4D8`); scraper chevron `group-hover:text-[#A1A1AA]` self-equal hover → repointed to `#71717A`; home quick-action card had a leftover `hover:bg-[#131313]` dark flash on a white card → repointed to `#FAFAFA`; home agent panel header still wrapped in `glass-dark` had its border + label inverted to light tokens by the bulk pass and was restored to dark recipe (`border-[#1A1A1A]`, `text-[#8E8E96]`); knowledge-base 'Agent's view (raw prompt block)' is a deliberate `bg-[#000000]` snippet container that had its border, hover and inner text inverted to light by the bulk pass — restored to dark recipe (`border-[#272727]`, `hover:bg-[#161616]`, `text-[#FAFAFA]/[#8E8E96]`); social-media had three leftover `hover:bg-[#0B0B0B]` row hovers (channels / tone / recurring) and a `text-[#D4D4D8]` audience label on the now-white cards — all repointed to the light recipe. The Replit project doc was rewritten to document three layers instead of two (sidebar + dark overlays / hub-chrome page bodies / per-brand pages) and the Site Scraper hub section was corrected from 'hub-chrome dark theme' to 'hub-chrome light theme'. Files NOT touched: `SidebarLayout.tsx`, `BrandAgent.tsx`, `content-calendar.tsx`, `brand-picker.tsx`, `index.css`. No data, schema or API changes.",
    ],
  },
  {
    sortKey: "2026-05-12-u",
    date: "2026-05-12",
    category: "Design",
    summary: "Global one-notch lift on the hub-chrome dark palette — the previous `#070707/#0A0A0A/#0E0E0E` recipe read as nearly black on most monitors and crushed the depth between layers, so every surface, border and small-text token used across hub-chrome pages was mapped up to a slightly warmer / lighter step that preserves the same depth ordering but breathes.",
    capabilities: [
      "Lifted every hub-chrome surface and border token by one step across 13 files (393 lines): canvas `#070707 → #0E0E0E`, primary panel `#0A0A0A → #121212`, surface `#0E0E0E → #161616`, panel-header `#0F0F0F → #161616` (now merges with surface for one fewer layer), alt surface `#111111 → #181818`, surface variant `#141414 → #1A1A1A`, input bg `#161616 → #1C1C1C`, subtle bg / divider `#1A1A1A → #222222`, sidebar border `#1F1F1F → #272727`, card border `#262626 → #2D2D2D`, hover-quiet border `#2A2A2A → #333333`, hover-strong border `#3A3A3A → #454545`. The remap was done with sentinel placeholders so the colliding hex values (e.g. old `#161616` becoming `#1C1C1C` while old `#0E0E0E` was simultaneously becoming `#161616`) substituted cleanly without double-application. Brand accent `#1e82b4`/`#39A15F`, the white text scale (`#FAFAFA`/`#E4E4E7`), and the medium muted `#A1A1AA` were intentionally left alone — the depth ordering and accent system stay identical.",
      "Raised the small-text contrast at the same time so the new lighter surfaces don't expose legibility issues on 10–11px labels that the architect flagged earlier: muted labels go `#71717A → #8E8E96` (now ~5.5:1 on `#161616`, comfortably above WCAG AA 4.5:1), and micro-copy / placeholder text goes `#52525B → #6B6B73` (now ~3.6:1 on `#161616`, AA Large) so helper text stays readable without competing with primary copy. Replit project doc updated to reflect the new token recipe under the Hub product chrome section so future passes use the lifted palette by default. Files touched: `content-calendar.tsx` (140 lines — the dark calendar shell + modals are hub-chrome inside this brand page), `scraper.tsx`, `nico.tsx`, `knowledge-base.tsx` (44 each), `social-media.tsx` (30 — the sticky channel bar is hub-chrome inside this brand page), `SidebarLayout.tsx` (15), `brand-picker.tsx` (14), `settings.tsx`, `settings-pillars.tsx` (13 each), `changelog.tsx` (11), `home.tsx`, `BrandAgent.tsx` (10 each), `index.css` (5). Pure-light per-brand pages (brand-identity, fleet, offers, brand-history, assets, content-ideas, copywriter, events, resources, travel-info, onboard-experience, saved, media-library, unique-selling-points, monthly-planning) and the historical changelog entries that document the old recipe were intentionally untouched. No data, schema or API changes.",
    ],
  },
  {
    sortKey: "2026-05-12-t",
    date: "2026-05-12",
    category: "Design",
    summary: "Rolled the dark hub-chrome aesthetic onto the Content Calendar modals (CardDetailModal + NewPostModal) and the shared inline pieces they reuse — every form surface, label, input, toggle and footer button now matches the calendar shell so opening a post no longer flashes a white sheet over the dark page.",
    capabilities: [
      "Repainted the two post modals end-to-end on the new dark recipe: backdrops moved to `bg-black/70 backdrop-blur-md`, panels to `bg-[#0A0A0A] ring-1 ring-[#1F1F1F]` with a soft `0_24px_60px_-12px_rgba(0,0,0,0.8)` shadow, sticky headers on `bg-[#0A0A0A]/90 backdrop-blur-md` with a 1px `#1A1A1A` hairline, and section dividers on `border-[#1A1A1A]`. Labels collapse to a 10px `#71717A` uppercase scale, body text reads `#E4E4E7` on `#161616` inputs (`border-[#262626]` idle → `border-[#1e82b4]/60` + `ring-[#1e82b4]/30` focus), helper micro-copy uses `#52525B`, placeholder text uses `#52525B`, and every native date/time/textarea input gains `[color-scheme:dark]` so the OS-rendered picker chrome (calendar popover, clock spinner, AM/PM) flips to dark instead of dropping a white surface back into the modal. The CardDetail title input keeps its brand-blue focus underline at `border-[#1e82b4]/60`, and the editable text/url/textarea variants in the shared `Editable` helper repaint matching dark surfaces with a hover border at `#1e82b4]/40`.",
      "Re-tinted every status colour layer used inside the modals to the alpha-on-dark palette so they sit on `#0A0A0A` without screaming: posted-URL inputs and the 'View live post' chip move to `emerald-500/10` fill, `emerald-500/20` border, `emerald-300/400` text + spinner; the approval block becomes `emerald-500/10` (approved) or `red-500/10` (rejected) with matching `300`-text headers; the same-day-conflict callout in NewPostModal becomes `amber-500/10` on `amber-500/20` with `amber-300` headline + `amber-200/90` rows; the upload dropzone gets a `border-[#1F1F1F]` dashed idle, brand-tinted hover, and an `emerald-500/10` 'done' state with `emerald-300` confirmation text. Buttons follow the hub-chrome convention: ghost actions use `#71717A → #E4E4E7` text with `#161616` hover, secondary actions like Cancel / Add-person / removal pill swap to `bg-[#161616] ring-1 ring-[#262626]`, the destructive 'Yes, delete' button stays solid red (`bg-red-500/90`), and the primary Save keeps the brand `#1e82b4 → #1a6d99` fill. The recurring-toggle card repaints to `border-[#1F1F1F] bg-[#0E0E0E]` (active: `border-[#1e82b4]/40 bg-[#1e82b4]/10`) with the track at `bg-[#262626]`, and the GHS variant's checkbox picks up `border-[#262626] bg-[#161616] [color-scheme:dark]` so the native check chrome stays dark too. The shared `MiniCalendar` (used inside NewPostModal's GHS picker) gets the same wrapper / nav / weekday / cell repaint, and the shared `PillSelect` label + saving spinner move to `#71717A`. The Status pill dropdown options inside CardDetailModal (Draft / Approved / Scheduled / Skipped) — which were still rendering as light pastel chips on `bg-amber-50 / bg-emerald-50 / bg-sky-50 / bg-slate-100` — were repainted to the same alpha-on-dark recipe (`bg-{tone}-500/10 text-{tone}-300 ring-1 ring-{tone}-500/25`, with Skipped on `bg-[#161616] text-[#A1A1AA] ring-[#262626]`) so the closed pill matches its surrounding chrome and the dropdown surface stays on one continuous dark plane. The 'Posted' option intentionally keeps its solid `bg-emerald-500` fill since it doubles as a status milestone signal. ImportHistoryModal and the typecheck-pre-existing `tone_register` line are intentionally untouched. No data, schema or API changes.",
    ],
  },
  {
    sortKey: "2026-05-12-s",
    date: "2026-05-12",
    category: "Design",
    summary: "Cleanup pass on the Content Calendar dark refactor — converted the four shared style helpers (creativeStatusConfig, marketBadge, eventPillColor, event-overflow counter) to dark-surface variants and added focus-visible rings to PostRow micro-controls.",
    capabilities: [
      "Replaced the remaining light-pastel chips on PostRow and CalendarGrid with dark-tinted variants so the row chrome reads as one continuous surface: creative-status pills now use `bg-emerald-500/10 text-emerald-300 ring-emerald-500/25` (and the matching sky/grey scales), event pills use the same alpha-on-dark recipe with light-300 text, the market badge gains a 25%-opacity ring plus a brighter `#5BB6E0`/`#FBC764` text colour for legibility at 10px on `#0E0E0E`, and the day-event overflow counter (`+N`) moves from `text-gray-400` to `text-[#71717A]`. The 'Approved' status dot picks up a soft green halo to match the row's status stripe.",
      "Added explicit `focus-visible` rings to the two PostRow micro-controls that previously had none: the compact (posted) row button gets `focus-visible:ring-2 ring-[#1e82b4]/50` plus `focus-visible:opacity-100` so it emerges from its 50% idle state when tabbed to, and the ghost add-channel buttons (`+IG`, `+FB`, `+Story`) get the same ring at 60% alpha and have their idle opacity bumped from 60% to 75% so the affordance is discoverable without hover. Lucide `title` props on the inline meta icons remain wrapped in `<span title>` to keep tooltips without TS friction. No data, schema or API changes.",
    ],
  },
  {
    sortKey: "2026-05-12-r",
    date: "2026-05-12",
    category: "Design",
    summary: "First pass of the dark hub-chrome aesthetic on the Content Calendar — page chrome, sticky toolbar, day rows, post rows and the floating selection bar all moved off the white surface onto the #070707/#0E0E0E palette.",
    capabilities: [
      "Re-skinned the Content Calendar shell to the new premium dark style: #070707 canvas with a subtle ambient radial overlay, sticky header on `bg-[#070707]/85 backdrop-blur-md` with a 1px #141414 hairline, sentence-case 'Content calendar' title at 15/16px, month nav and `Past` chip recoloured to #52525B/#71717A. The market/platform filter strip becomes a dark pill on `bg-[#0E0E0E] border-[#1A1A1A]` while keeping the brand-coloured active states (IG gradient, FB blue, EN Virtu blue, IT red) so the channel cues read instantly. Toolbar action buttons (Export, View posted, View past, Import history, Share, Add post) repainted as quiet 11px ghost buttons with #161616 hover, and the primary 'Add post' button picked up a soft brand glow.",
      "Re-skinned the calendar body and post rows: day separators on `divide-[#141414]`, weekday/date label muted to #52525B with the today pill keeping the brand `#1e82b4` fill plus a soft halo, day-row hover on `bg-[#0E0E0E]`, weekend empty days dimmed to 30% opacity. PostRow swapped to `bg-[#0E0E0E] border-[#1A1A1A] hover:border-[#262626]` with a glowing emerald status stripe; titles at 13px on #E4E4E7, secondary line at 11px #52525B with the time stamp accented in brand blue. Compact (posted) rows, the unscheduled callout (now amber-tinted dark glass), and the floating selection bar (`bg-[#0E0E0E]/95 backdrop-blur-xl border-[#1F1F1F]`) all match. Modals (CardDetail, NewPost, MiniCalendar, Editable) are intentionally out of scope on this pass and kept on the existing light palette — they get their own dedicated round so the heavy form chrome doesn't get rushed. No data, schema or API changes; pre-existing typecheck noise around `tone_register` and Lucide `title` props is unaffected (a few `<Icon title>` usages on this page were wrapped in a `<span title>` so the dark pass doesn't add any new typecheck noise).",
    ],
  },
  {
    sortKey: "2026-05-12-q",
    date: "2026-05-12",
    category: "UX",
    summary: "Layered real UX onto the Social Media strategy page — sticky section nav with active-state tracking, instant-orientation stat strip, and a proper 3-column data table for Cadence.",
    capabilities: [
      "Added a sticky section nav under the hero (`/social-media`) that auto-builds from whichever sections actually have content (01–07). Uses `IntersectionObserver` with a `-20% / -60%` rootMargin to highlight the section in view; clicking a chip smooth-scrolls to it with a 90px offset to clear the sticky bar. Active chip gets a brand-colored numeric prefix and a #161616 fill, inactive chips are quiet #71717A on transparent. Bar uses `bg-[#070707]/80 backdrop-blur-md` so content scrolls under it cleanly. Added a `.scrollbar-none` utility in `index.css` so the nav scrolls horizontally on narrow widths without showing a scrollbar.",
      "Added an at-a-glance stat strip directly under the hero ('3 markets · 9 accounts · 8 pillars · 4 tone registers · 5 recurring slots') so users orient in under a second instead of scrolling. Rebuilt the Cadence section as a real 3-column data grid (Channel · Market · Cadence) with column headers, row hover, and a `↳` glyph for repeated channel rows so the visual grouping reads like a spreadsheet. Tone-Register swatches upgraded from a 3px rail to a glowing 8px dot; example copy now wrapped in proper smart quotes. Pillar cards reveal a brand-colored 2px left rail on hover. No data, schema, or API change.",
    ],
  },
  {
    sortKey: "2026-05-12-p",
    date: "2026-05-12",
    category: "Design",
    summary: "Refined the Social Media strategy page to a much more restrained, Apple/Linear-style scale — smaller hero, tighter spacing, sentence case, trimmed copy. The previous pass was too shouty and chunky.",
    capabilities: [
      "Hero shrunk from 56px uppercase display down to a 28px sentence-case 'Social media' with a single one-liner subtitle ('Where we publish, how often, and how we sound.') instead of the long paragraph. Section headers collapsed from 3-line stacks (eyebrow + h2 + subtitle + hairline) into a single inline row: numbered eyebrow (01–07) + tiny sentence-case title + small icon, all on one 13px line. Container narrowed from `max-w-5xl` to `max-w-4xl` and section spacing dropped from `space-y-20` to `space-y-12`.",
      "Cards rebuilt at a smaller scale: rounded-xl (was rounded-2xl), padding p-3.5/p-4 (was p-5/p-6), body text 12–13px (was 14px), no per-card glows or oversized icons. Cadence, Tone, Recurring sections collapsed into single grouped cards with `divide-y` rows instead of one card per item. Brand-color accents preserved (handles, pillar numbers, recurring chips, seasonal icons, list markers) but at lighter weight. No data, copy, or schema change — same content, restrained presentation.",
    ],
  },
  {
    sortKey: "2026-05-12-o",
    date: "2026-05-12",
    category: "Design",
    summary: "Test-rolled the new premium dark hub-chrome aesthetic onto the Virtu Ferries Social Media strategy page — first per-brand page to inherit the landing/dashboard treatment, with brand colors preserved as accents on a near-black canvas.",
    capabilities: [
      "Social Media page (`/social-media`) is now full hub-chrome dark: #070707 canvas with the same ambient radial backdrop as landing/dashboard, hero set in uppercase Montserrat bold ('SOCIAL MEDIA.' with 'Media.' in muted #52525B), all SectionHeaders rebuilt with eyebrow + uppercase title + dark hairline divider. Every card moved to #0F0F0F surfaces with #1A1A1A borders and on-hover #2A2A2A — Platforms cards even pick up a soft brand-tinted glow on hover that uses `color-mix` against `--brand-primary`",
      "Brand-color injection preserved end-to-end so the page still feels like Virtu — handles, market notes, recurring-post chips, seasonal icon backgrounds, content-pillar numbers, list markers, and the cross-posting hashes all use `var(--brand-primary)` (and `--brand-alert` for the platform-specific list). Tone-Register cards keep their per-register color but as a glowing left rail (matching the new sidebar nav rail). Cadence numbers use `.num-tabular`. No data, schema, route, or business-logic change — same content, premium dark wrapper.",
    ],
  },
  {
    sortKey: "2026-05-12-n",
    date: "2026-05-12",
    category: "Design",
    summary: "Tightened the landing and dashboard heroes — full-caps Tesla/Apple-keynote treatment, dropped the Instrument Serif italic in favour of a same-weight muted accent for cleaner tonal contrast.",
    capabilities: [
      "Brand picker hero is now 'ONE HUB. EVERY BRAND.' rendered in uppercase Montserrat bold with the word 'Every' in a muted #52525B for in-family tonal contrast (no second font, no italic) — reads as more confident and on-brand for a premium product hub",
      "Dashboard hero mirrored the same treatment — 'YOUR BRAND, ON CALL.' in uppercase bold with 'On Call.' as the muted accent — and the upper-case tracking was dialed in (-0.02em / -0.015em) so the heavier letterforms still feel tight and architectural at hero scale",
    ],
  },
  {
    sortKey: "2026-05-12-m",
    date: "2026-05-12",
    category: "Design",
    summary: "Premium 360 redesign of the hub's first-impression surfaces — landing (brand picker), dashboard, and global sidebar chrome — for an Apple × Revolut × Tesla feel within three seconds of opening the app.",
    capabilities: [
      "Foundation: added Instrument Serif as a display accent font for italic 'wow' moments (paired with Montserrat sans for the body), introduced a new premium type scale (`.h-hero`, `.h-hero-md`, `.display-serif`, `.num-tabular`) and shared utility primitives (`.glass-dark` panel, `.ambient-radial` background, `.ambient-lines` vertical texture, `.pill-nav` chrome button, `.nav-active-rail` glowing left rail). All additive — every existing brand page (`.h-display`, `.surface-card`, etc.) is untouched, so the dense reference content keeps its current density",
      "Brand picker (landing): full hero rebuild — ambient radial glow + faint vertical-line texture behind the content, deeper near-black canvas (#070707), top pill nav, bold Montserrat hero with Instrument Serif italic accent ('One hub. *Every* brand.'), larger 3xl-radius brand cards with on-hover brand-tinted glow, refined entry chip with sparkle, animated entrance with custom cubic easing. Dashboard: matching ambient backdrop, hero with 'Your brand, *on call.*' (sans + serif italic), upgraded quick-action tiles with icon badge + hint copy, agent surface wrapped in a glass panel with status header ('Live' indicator). Sidebar: tighter, more elegant chrome — refined brand block with eyebrow, gradient initials chip, active items now use a glowing left accent rail instead of a solid tinted pill, smaller children, refined footer team chip with status dot. Backwards-compatible: routes, business logic, and brand-page content untouched",
    ],
  },
  {
    sortKey: "2026-05-12-l",
    date: "2026-05-12",
    category: "Strategy",
    summary: "Added a 'Seasonal Themes' section to the Social Media strategy page — a year-on-a-page view of what Sicily and the crossing feel like in each season, so posts get anchored to the right mood, topic, and visuals for the time of year they land.",
    capabilities: [
      "Added typed `seasonalThemes: SeasonalTheme[]` to SocialMediaContent (lib/brand-knowledge) with four entries for Virtu — Spring (Mar–May): Nature, Parks, Mountains, Blooming Sicily, Easter, Fresh starts; Summer (Jun–Aug): Sea, Beaches, Heat, Festivals, Summer body content; Autumn (Sep–Nov): Food harvest, Wine, Quieter Sicily, Softer light; Winter (Dec–Feb): Festive Sicily, Etna snow, Christmas markets, New Year. Rendered as a 4-card grid on the Social Media page with seasonal icons (Flower2/Sun/Leaf/Snowflake) and pill-tagged themes",
      "Wired Seasonal Themes into formatBrandKnowledgeAsPrompt as a labelled block under 'Social media reference' so the AI agent now ships the full year framing as part of its system prompt, and surfaced it in the Knowledge Base page so it's visible alongside the other agent-fed strategy. Restarted the API server to regenerate the snapshot and bundle the new prompt",
    ],
  },
  {
    sortKey: "2026-05-12-k",
    date: "2026-05-12",
    category: "Resources",
    summary: "Added a 'Sicily Towns' atlas under Resources — every town reachable within 4 hours of Pozzallo, grouped by drive time, so the team has one canonical reference for destination content and recommendations.",
    capabilities: [
      "Promoted Resources to a sidebar group with two children: Library (the existing /resources page) and the new /sicily-towns page. The new page is per-brand (Virtu only — Pozzallo is the Virtu terminal) and lives at /sicily-towns inside the BrandGuard. Light brand-themed UI with four time brackets (Under 1h, 1–2h, 2–3h, 3–4h), each town as a small card with a short descriptor (Noto, Modica, Ragusa Ibla, Ortigia, Catania, Taormina, Palermo, Trapani, San Vito Lo Capo, Messina, etc.). Empty Sicily Towns content for Gozo Highspeed renders an EmptySection rather than blowing up",
      "Added typed `sicilyTowns: SicilyTownsContent` (groups → towns → name/description) to BrandContent in lib/brand-knowledge, populated for Virtu Ferries with all 26 towns from the brief. Wired the same structure into formatBrandKnowledgeAsPrompt so the agent now ships with the full drive-time atlas as part of its system prompt under its own block. Surfaced in the Knowledge Base page's 'Social media reference' card with the towns grouped by bracket, and rebuilt the api-server so the data-snapshot regenerates and prod will sync on the next publish",
    ],
  },
  {
    sortKey: "2026-05-12-j",
    date: "2026-05-12",
    category: "Strategy",
    summary: "Added a Recurring Content strategy section to the Social Media page — standing posts that hit the calendar every week on the same cadence. First entry: the Saturday Weekly Schedule.",
    capabilities: [
      "Extended SocialMediaContent in lib/brand-knowledge with a new typed array `recurringPosts: RecurringPost[]` (cadence, day, title, what, optional market/channel/notes). Renders as a new 'Recurring Content' section on the per-brand Social Media page sitting between Tone Registers and Cross-Posting Logic, using the brand-primary accent and the CalendarClock lucide icon. Empty for Gozo Highspeed (the section just doesn't render); seeded for Virtu Ferries with the Saturday Weekly Schedule rule (next-week Mon→Sun sailings, both directions, both ports, on both Maltese FB+IG and Italian FB, treated as service content with the Operational / Disruption register)",
      "Fed the same recurring-post list into the AI agent's system prompt via formatBrandKnowledgeAsPrompt — appears under 'Recurring content (always-on calendar slots)' inside the existing 'Social media reference' block. Now when anyone asks the brand agent about cadence, the Saturday weekly-schedule slot is part of its knowledge instead of having to be re-explained each time. No DB or migration changes — it's a static brand-knowledge edit",
    ],
  },
  {
    sortKey: "2026-05-12-i",
    date: "2026-05-12",
    category: "Social Media",
    summary: "Added 'Scheduled' as a post status — the in-between state for posts that are approved AND queued in the publishing tool but haven't actually gone live yet.",
    capabilities: [
      "Extended the PostStatus union in content-calendar.tsx with 'scheduled' (slotted between 'approved' and 'posted'). It appears as a new sky-blue chip in the status switcher inside CardDetailModal and as a 'Scheduled' option in both the new-post and edit-post dropdowns. Uses the Calendar lucide icon and bg-sky-100/text-sky-700 styling so it's visually distinct from the green Approved/Posted chips",
      "No DB migration needed — content_posts.status is a free-form text column (default 'pending') with no enum constraint, and the rest of the codebase already pattern-matches on the known values with sensible Draft fallbacks. Existing rows are unaffected; the new value can be selected immediately on either brand",
    ],
  },
  {
    sortKey: "2026-05-12-h",
    date: "2026-05-12",
    category: "Workspace",
    summary: "Pre-loaded the Content Calendar 'Assigned to' dropdown with the four people who actually use the hub — Calvin Sam, Shania Catania, Ayrton Galea, Nico Bazan — for both Virtu Ferries and Gozo Highspeed.",
    capabilities: [
      "Inserted the four standard team members into the team_members table for brand_id 1 (Virtu Ferries) and brand_id 2 (Gozo Highspeed) via idempotent ON CONFLICT (brand_id, name) DO NOTHING upserts. Calvin Sam, Shania Catania and Ayrton Galea have no role; Nico Bazan is tagged 'Videographer'. Existing legacy members on brand_id 1 (Gabrielle Spiteri, Luis Marquez) were left in place — nothing was deleted",
      "Because team_members is in the snapshot dump+bootstrap list, the next prod build will sync these rows automatically. The synthetic 'Nico Bazan' fallback in content-calendar.tsx is kept as a no-op safety net (deduped by name) so the dropdown still works even if the team_members fetch fails",
    ],
  },
  {
    sortKey: "2026-05-12-g",
    date: "2026-05-12",
    category: "Workspace",
    summary: "Promoted Nico's drop-zone from a Virtu-only sidebar entry to a hub-level page available from the brand-picker. Posts assigned to 'Nico Bazan' on any brand's calendar now also surface on his page.",
    capabilities: [
      "Removed the per-brand 'Nico' sidebar entry. The page now lives at /nico OUTSIDE the BrandGuard (alongside /share/:token in App.tsx) so it's reachable without first picking a brand. Added a small Nico pill in the brand-picker header that links to it. The page itself was reskinned to the dark hub-chrome theme (bg #0A0A0A, accent #39A15F) instead of the Virtu blue, since it no longer belongs to a single brand",
      "Made nico-links cross-brand: GET /api/nico-links and PATCH/DELETE no longer filter by req.brandId, and POST always inserts brand_id=1 (legacy column kept for backwards compat). Added a new GET /api/nico-posts endpoint that returns every content_post where assigned_to ILIKE 'Nico Bazan' across ALL brands, joined with the brand's name + primaryColor for badges. The Nico page renders these as a 'Posts tagged for you' section above the asset link table, each card colour-stripped with its brand colour and showing platform/pillar/scheduled date. Nico Bazan is now always offered as an assignee in the Content Calendar's new-post / edit-post forms, even though he isn't a row in any brand's team_members table (synthetic option with id -1 prepended in content-calendar.tsx)",
    ],
  },
  {
    sortKey: "2026-05-12-f",
    date: "2026-05-12",
    category: "Social Media",
    summary: "Renamed the 'English Market' to 'Maltese Market' across the entire Virtu Ferries hub, and trimmed redundant language out of the cadence labels (Italian Facebook is just '25 posts per month', English Instagram is just '25 posts per month' — no more '· Italian' or '· English · Maltese audience' tail).",
    capabilities: [
      "Renamed 'English Market' → 'Maltese Market' in the brand-knowledge registry, the agent prompt label, the Content Calendar new-post / edit-post dropdown options, the badge logic, the skipped-posts page, and the api-server prompt builder for AI post generation. All 38 existing content_posts rows in the DB (37 'English Market' + 1 stray 'English') were UPDATEd to 'Maltese Market' so the calendar still shows them",
      "Cadence strings cleaned: Italian FB cadence is now just '25 posts per month' (was '25 posts per month · Italian' — redundant since the market is already labelled Italian). English IG cadence is now just '25 posts per month' (was '25 posts per month · English · Maltese audience' — redundant since the audience/frame is already shown on the same Maltese Market card)",
    ],
  },
  {
    sortKey: "2026-05-12-e",
    date: "2026-05-12",
    category: "Social Media",
    summary: "Flipped the Posting Cadence section so it groups by channel instead of by market. One row per platform (Facebook, Instagram, …) with each market that posts on it listed underneath alongside its handle and target volume.",
    capabilities: [
      "Cadence is now built by walking every market's platform list and bucketing into a Map keyed by platform name. Each channel card has the platform icon + name as a header and a clean divider list below showing market → handle → cadence",
      "User benefit: scanning 'how often per channel' is now one read (look at Facebook, see English 25/mo and Italian 25/mo together) instead of two (read English market, then read Italian market and mentally rejoin)",
    ],
  },
  {
    sortKey: "2026-05-12-d",
    date: "2026-05-12",
    category: "Social Media",
    summary: "Reworked the Social Media strategy page so each topic gets its own clearly separated section instead of being squashed into one block. Platforms and Posting Cadence are now two distinct sections, and every section has a proper header with an eyebrow label, an icon, a bold title and a hairline rule — no more tiny coloured dashes.",
    capabilities: [
      "Platforms section now shows only the channels and handles per market (no cadence noise). Each market is its own card with audience + framing + an optional brand-tinted note pinned underneath",
      "Posting Cadence is a brand-new section showing each platform's posting volume in a clean two-column grid, grouped by market, so the user can scan 'how often per channel' at a glance without re-reading the platforms list",
      "Section dividers replaced: the old 8×2px coloured dash next to each h2 is gone. Every section now has a SectionHeader with uppercase eyebrow + lucide icon, a vertical accent bar, the bold title, an optional one-line subtitle, and a fading hairline rule underneath. Sections are also further apart vertically (space-y-20)",
      "Content Pillars use zero-padded numerals (01, 02, …) in the brand primary tint instead of near-invisible white/10. Cross-Posting bullets now show actual disc markers in brand colour instead of unmarked lines. No data/schema changes — purely a presentation rewrite",
    ],
  },
  {
    sortKey: "2026-05-12-c",
    date: "2026-05-12",
    category: "Gozo Highspeed",
    summary: "Added a Hop-On Hop-Off page to the Gozo Highspeed brand hub. Sidebar entry under Brand → Hop-On Hop-Off (GHS only). Page focuses on the three things that actually matter to a customer: prices, the 14 bus stops, and the daily timetable for each run.",
    capabilities: [
      "New page at /hop-on-hop-off with three sections: prices (€30 adult / €20 child 4–10 / FREE infant 0–3, 1-day validity), the 14-stop loop starting and ending at Mġarr Harbour, and a sticky-first-column timetable showing all 9 daily departure runs",
      "GHS palette throughout — primary blue #1d3289 for stop numbers and accents, red #ea2d3f reserved for the seasonal note that the 15:45 Mġarr Harbour departure does not operate between November and March",
      "Sidebar route is brand-conditional (only appears when active brand is Gozo Highspeed) so it does not pollute the Virtu Ferries hub. Page itself shows a graceful EmptySection if visited under a non-GHS brand",
    ],
  },
  {
    sortKey: "2026-05-12-b",
    date: "2026-05-12",
    category: "Content Calendar",
    summary: "Added a one-click \"add another channel\" action right on every post row in the Content Calendar. After saving a post as Facebook-only (or Instagram-only, or any single channel), the channels you have NOT picked appear as faint icons next to the active platform — click one to instantly publish the same post on that channel too, no need to open the edit modal.",
    capabilities: [
      "PostRow now renders ghost icons for every unselected channel beside the active platform icons. Each ghost icon has a small + badge in the corner so the affordance reads as 'add this'. Click → PATCH /api/content/posts/:id with the new platform string and refetch the month",
      "Virtu Ferries logic: English-market posts can flip Facebook ↔ Both (FB+IG) or Instagram ↔ Both with a single click; cross_post is set to true. Italian-market posts stay FB-only (no add icon shown) since the brand only operates Italian on Facebook",
      "Gozo Highspeed logic: comma-separated platform list (Facebook,Instagram,Story) supports any combination. Adding a channel appends to the list; the row immediately reflects the new icons after the refetch",
      "Hidden in selection mode and on profile-change entries (which have no platform). Loading spinner replaces the icon while the PATCH is in flight, and click events stopPropagation so the row's open-edit handler doesn't fire",
    ],
  },
  {
    sortKey: "2026-05-12-a",
    date: "2026-05-12",
    category: "Content Calendar",
    summary: "Made image/video uploads on the Gozo Highspeed Content Calendar a first-class action. The attachment field is now visible by default on every new GHS post and uses the GHS palette so it stops feeling like a Virtu Ferries control bolted on.",
    capabilities: [
      "New posts on the GHS calendar default to attachment_type=\"upload\" so the dropzone is visible immediately — no need to discover the toggle. Virtu Ferries keeps its existing default of \"none\"",
      "Attachment toggle (None / Upload / Link) and dropzone hover state now use GHS Brand Blue (#1d3289) when the active brand is Gozo Highspeed; Virtu Ferries keeps #1e82b4. The active brand's identity now flows into the calendar form instead of being overridden",
      "Field label on GHS reads \"Visual — image or video — optional\" instead of the generic \"Attachment (optional)\", matching how the GHS team talks about creative",
      "GHS dropzone reformatted to the same vertical, centred layout as Virtu (icons + 'Click to upload an image or video' + accepted formats) so the affordance is unmissable",
    ],
  },
  {
    sortKey: "2026-05-11-e",
    date: "2026-05-11",
    category: "Brand Identity",
    summary: "Typography section on the Assets page is now a full weight ladder. Each Montserrat weight has its own card showing the weight name, a real sample line rendered at hero scale in that exact weight, and a clear \"use for\" guideline so designers know when to reach for which weight.",
    capabilities: [
      "TypographyBlock weights now carry a `usage` field (Virtu Ferries: Light → ExtraBold; Gozo Highspeed: Black → Regular). Usage strings explain the role of each weight (hero claim vs section heading vs body) so the choice is unambiguous",
      "Assets page Typography section rebuilt — replaced the old two-column \"first three / last three\" split with a single ladder, one card per weight, sample line rendered at text-3xl/4xl in the actual weight so the visual hierarchy is obvious at a glance",
      "Agent prompt formatter (formatAssets in lib/brand-knowledge/src/prompt.ts) updated to emit the per-weight usage line alongside the sample, so the AI agent now knows which weight to recommend in copy briefs",
    ],
  },
  {
    sortKey: "2026-05-11-d",
    date: "2026-05-11",
    category: "AI Agent",
    summary: "Injected the FULL Gozo Highspeed Brandbook (every page of the 2024 PDF) into the AI agent's knowledge base. The prompt formatter now also dumps the visual identity block (colours, typography, logo lockups, do's and don'ts) for every brand, so the agent can reason about palette, font, and logo-usage rules — not just copy.",
    capabilities: [
      "Prompt formatter (lib/brand-knowledge/src/prompt.ts) extended with a new \"Assets & visual identity\" section that lists brand colours (with hex + CMYK descriptions), typography weights with sample lines, logo lockup variants, logo mark anatomy (where present), and the do's / don'ts. Applies to BOTH brands automatically — Virtu Ferries agent now also gets its palette, Mediterranean V/A/B mark anatomy, and logo rules in-prompt",
      "Gozo identity expanded with full brandbook personality block — Reliable, Efficient, Customer-Centric, Friendly each with its rationale — surfaced via keyMessages so the agent can quote them",
      "Gozo tone of voice rewritten verbatim from the brandbook (Clear / Professional / Engaging / Energetic with full descriptions)",
      "Gozo brand story now matches the brandbook's Brand Introduction page word-for-word, including the closing mantra line",
      "Gozo assets.logos populated with the six official lockups (icon, wordmark, wordmark + Operated-By tagline — each in full-colour and white-reversed) so the agent knows which mark to recommend in which context",
      "Gozo logo don'ts expanded from 4 to 7 entries to cover all six brandbook \"Logo Incorrect Uses\" (move/shrink elements, change colours, warp, stretch, drop shadow, add/remove elements) plus the existing background-contrast rule",
      "Hero claims from the brandbook mockups (\"Malta to Gozo in 45 Minutes\", \"Valletta → Gozo in Only 45 Minutes\", ferry-schedule poster format) added to keyMessages so the agent can echo them in copy",
    ],
  },
  {
    sortKey: "2026-05-11-c",
    date: "2026-05-11",
    category: "Brand Identity",
    summary: "Loaded the official Gozo Highspeed Brandbook (2024 edition) into the hub. Brand Identity, Assets palette, and the AI agent's system prompt now reflect the real brand — personality (Reliable / Efficient / Customer-Centric / Friendly), tone (Clear / Professional / Engaging / Energetic), positioning, mantra, and the official two-colour palette.",
    capabilities: [
      "Brand Identity page populated with the brandbook story, four tone-of-voice cues, key messages (incl. positioning + mantra), and explicit do/don't copy rules — all auto-feed into the agent system prompt via formatBrandKnowledgeAsPrompt",
      "Assets page now shows the official two-colour palette only — Brand Blue #1d3289 (C100 M94 Y13 K2) and Brand Red #ea2d3f (C2 M96 Y77 K0) — plus a Montserrat (Black → Regular) typography block and brandbook-derived logo do/don't list",
      "Brand picker chrome (sidebar accents, share-link headers) refreshed for Gozo Highspeed: primary #1d3289, accent #ea2d3f. seedBrandsIfMissing now updates the brands row when palette/tagline drift from the canonical seed, so dev and prod stay in sync without a manual SQL push",
      "AI system prompt for Gozo rewritten around the brandbook's four personality pillars + tone + positioning + mantra, and explicitly forbids reusing Virtu Ferries (Malta ↔ Sicily) copy",
    ],
  },
  {
    sortKey: "2026-05-11-b",
    date: "2026-05-11",
    category: "Hub Cleanup",
    summary: "Moved 'Saved for Later' under the Social Media group in the sidebar (both brands) and renamed it to 'Ideas' so the inspiration bin sits next to where it actually feeds — between Strategy and Content Calendar.",
    capabilities: [
      "Sidebar 'Social Media' group is now: Strategy / Ideas / Content Calendar / Skipped Posts",
      "Top-level 'Saved for Later' nav entry removed; the underlying /saved route, page, and saved_items DB table are unchanged so existing bookmarks and links still work",
    ],
  },
  {
    sortKey: "2026-05-11-a",
    date: "2026-05-11",
    category: "Hub Cleanup",
    summary: "Temporarily removed four under-baked features from the sidebar of both brands — Social Expert, Content Ideas, Monthly Planning, and Copywriter — so the hub stays focused on what's actually being used day-to-day. Underlying API routes and DB tables are untouched, so the features can be brought back without data loss when they're rebuilt.",
    capabilities: [
      "Sidebar 'Social Media' group on both Virtu and Gozo Highspeed now shows only: Strategy / Content Calendar / Skipped Posts (Social Expert, Content Ideas, Monthly Planning, Copywriter entries removed)",
      "Page files removed: social-media-expert.tsx, content-ideas.tsx, monthly-planning.tsx, copywriter.tsx, copywriter-library.tsx, copywriter-rules.tsx, copywriter-combined.tsx; corresponding routes (/social-media-expert, /content-ideas, /monthly-planning, /copywriter, /copywriter-library, /copywriter-rules) deleted from App.tsx so the URLs now 404 inside the SPA",
      "Server-side endpoints, copywriter rules/feedback DB tables, and content_ideas table left in place — no destructive backend changes — so reintroducing the features later just means putting the React pages back",
    ],
  },
  {
    sortKey: "2026-05-07-b",
    date: "2026-05-07",
    category: "Content Calendar",
    summary: "Added a 'Skipped' status alongside Draft / Approved / Posted in the Content Calendar (both Virtu and GHS). Skipped posts disappear from the calendar grid and live on a new /skipped-posts archive page where they can be restored as drafts or deleted permanently.",
    capabilities: [
      "Status pill in the post detail modal now offers Draft / Approved / Posted / Skipped on both brands",
      "Skipped posts are hidden from the calendar's monthly view so they don't clutter planning, but the underlying row is preserved in content_posts with status='skipped'",
      "New 'Skipped Posts' sidebar entry under Social Media (both brands) opens a brand-scoped table — Date / Channel / Title&Caption / Pillar — sorted newest-first across all months",
      "Each row has Restore (sets status back to 'pending'/Draft, returns to calendar) and Delete (permanent) actions; new GET /api/content/posts/skipped endpoint serves the archive",
    ],
  },
  {
    sortKey: "2026-05-07-a",
    date: "2026-05-07",
    category: "Internal Tools",
    summary: "Added a dedicated 'Nico' page for our videographer to drop raw asset links into. Plain table — Type / Date / Link — the team picks them up from there. Brand-scoped (Virtu only in the sidebar today, but the table is generic).",
    capabilities: [
      "New /nico page in the Virtu sidebar with a table view (Type, Date, Link, optional notes) and an Add button",
      "Backed by a new nico_links table and /api/nico-links CRUD endpoints (GET / POST / PATCH / DELETE), brand-scoped via x-brand-slug like every other resource",
      "Type field is open text on the server (so Nico can type anything if he needs to) but the UI offers Video / Voiceover / Image / Audio / Other as quick-pick chips",
      "Treated as user-content in the prod bootstrap — once prod has rows, dev edits don't overwrite them",
    ],
  },
  {
    sortKey: "2026-04-25-f",
    date: "2026-04-25",
    category: "Excursions",
    summary: "Enriched the four detailed Sicily excursions (Mt. Etna & Catania Winter, Ragusa Ibla / Modica / Scicli Winter, Taormina & Mt. Etna Summer, Catania & Mt. Etna Summer) with full schedule, pricing (incl. EU ETS breakdown), itinerary, local transport add-on and operational caveats. Added a 'Malta Excursion (inbound)' entry for the day-trip we sell to Sicilian and Italian visitors. Auto-feeds into the agent prompt so service-led answers ('when does Etna run?', 'how much for two adults and a child?', 'is the bus included?') are grounded in the published catalogue. Excursion type extended with optional schedule / pricing / itinerary fields.",
    capabilities: [
      "Mt. Etna & Catania (Winter, Oct 2025 – Apr 2026) — Adults €157.60, Children €101.60 (4 to under 14), Under 4 FREE; underlying €153 + €4.60 ETS / €97 + €4.60 ETS; min 10 pax. Thursdays + Sundays (1 Oct 2025 – 4 Jan 2026 and April 2026); Sundays 5 Jan – 31 Mar slot is taken by Ragusa/Modica/Scicli instead",
      "Mt. Etna & Catania schedule — Thursdays: DEP MLA 06:30 / ARR POZ 08:15 / DEP POZ 21:30 / ARR MLA 23:15. Sundays: DEP POZ 20:30 / ARR MLA 22:15",
      "Ragusa Ibla, Modica & Scicli (Winter, Oct 2025 – Apr 2026) — Adults €137.60, Children €97.60; underlying €133 + €4.60 ETS / €93 + €4.60 ETS; min 10 pax. Sundays (5 Jan – 31 Mar 2026): DEP MLA 06:30 / DEP POZ 19:30 / ARR MLA 21:15. Wednesdays (11 Mar – 29 Apr 2026): DEP MLA 07:30 / DEP POZ 19:30",
      "Taormina & Mt. Etna (Summer, 7 May – 27 Sep 2026) — Adults €159, Children €110 (incl. EU ETS), Under 4 FREE; min 15 pax. Thursdays (7 May – 21 Jun) + Sundays (3 May – 27 Sep). Catamaran returns at 21:30",
      "Catania & Mt. Etna (Summer, 25 Jun – 24 Sep 2026) — Adults €159, Children €110 (incl. EU ETS), Under 4 FREE; min 15 pax. Every Thursday: DEP MLA 07:30 / ARR POZ 09:15 / DEP POZ 21:30 / ARR MLA 23:15",
      "All excursions — Local transport in Malta (Hotel/Harbour/Hotel) is an optional €15 per person add-on (VAT incl.); pick-up points and timings on virtuferries.com",
      "All excursions are subject to fuel surcharge (Company reserves the right without notice), itinerary may change for weather, Conditions of Carriage apply",
      "All excursions include: arrival at Pozzallo, air-conditioned coaches with English-speaking guides, sampling stop for wines/almond sweets/honey on the Etna route (no obligation to buy), High-Speed Catamaran return",
      "Etna stop is the Sylvestri Crater at 2,000m; Catania highlights: Piazza del Duomo, Via Etnea, fish market, Roman Amphitheatre, Villa Bellini",
      "Ragusa/Modica/Scicli highlights: Gagliardi's Duomo (Ragusa Ibla), Modica Bassa with Duomo di San Giorgio (Cioccolato di Modica), Scicli with Palazzo Beneventano",
      "Taormina highlights: 206m above sea level, Greek Theatre (Hellenic 4 BC, summer classical plays), Corso Umberto, panoramic view over Giardini Naxos",
      "Syracuse & Marzamemi (Summer) — kept in catalogue but schedule/pricing/itinerary not yet captured; agent must verify the live site before quoting specifics",
      "Malta Excursion (inbound, year-round) — added as a day trip for Sicilian and Italian visitors travelling FROM Pozzallo TO Malta. Sell Malta as: compact, English-speaking, history-soaked archipelago; Independence 1964, EU since 2004; megalithic temples that predate the Pyramids and Stonehenge (UNESCO); Maltese is the only Semitic language using the Latin alphabet; long shared history with Sicily and Southern Italy in the Kingdom of the Two Sicilies until 1530 (when the Knights of St John arrived). Schedule and pricing not yet captured — verify live site",
    ],
  },
  {
    sortKey: "2026-04-25-e",
    date: "2026-04-25",
    category: "Onboard Experience",
    summary: "Expanded the Virtu Ferries Onboard Experience knowledge slot with the full 'Off We Go' on-board guide — boarding flow and decks, lounges and seating, outside seating / shop / entertainment, cafeterias and bars, family / car / pet travel, and practical info (smoking, charging, Purser). Auto-feeds into the agent prompt so service-led answers (where to sit for the best view, where to find the bar, what to do with kids on board, etc.) are grounded in the published guide.",
    capabilities: [
      "Boarding: vessel entrance is on Main Deck (garage deck) via accommodation gangway; lift available between Main and Upper Decks (ask Cabin Crew); reduced-mobility / pushchair passengers board on Main Deck via the garage ramp — notify staff at check-in",
      "Decks: Upper Deck has 5 lounges — Fore (front, best sea views), Aft (rear), Starboard (right), Port (left), and St. Elmo amidships (RESERVED for commercial vehicle drivers). Bridge Deck has the exclusive Club Lounge",
      "Seating: free choice in every lounge; capacity scaled down to 800 (vessel can take 1,000) for comfort, outdoor seating over and above; mostly reclining seats, small number of armchairs and sofas",
      "Stability tip: in adverse sea conditions the most stable seats are in the Starboard and Port Lounges, on the side furthest from the windows",
      "Outside Seating Areas open throughout the voyage — Grand Harbour Valletta view leaving Malta, Sicilian coast view leaving Pozzallo",
      "Hybleum on-board shop — fragrances 20% less than shops, packages only available in restricted travel retail; souvenirs, toys, wines, spirits, costume jewellery; coach tickets and taxi bookings to anywhere also sold here",
      "On-board entertainment: slot machines (regulated), movies on screen",
      "Cafeterias / Bars: 3 on board (Fore Lounge, Aft Lounge, Club) — hot/cold snacks, soft drinks, wines/spirits; every bar offers sweet and savoury gluten-free options",
      "Travelling with children: must be supervised by an adult at all times incl. Outside Seating Area; nappy changer on board (ask Cabin Crew); free colour crayons and colouring material from Cabin Crew, for the child to keep",
      "Travelling by car: drive-up check-in (no need to leave the vehicle); on arrival, drivers are called over PA to proceed to garage ahead of other disembarking passengers; 3 Main Deck exits closest to the car — Starboard Lounge, Port Lounge, far end of Aft Lounge",
      "Travelling with a pet: must inform staff at check-in AND a Cabin Crew member on boarding; refer detailed enquiries to the published Pet Policy",
      "Smoking: NOT permitted anywhere on the vessel except the designated area in the Outside Seating Area",
      "Mobile charging: designated charging points on board — ask the Cabin Crew",
      "Club Class additions: dedicated Club Lounge on Bridge Deck with reclining seats + footrests; opens onto its own Outside Seating Area; complimentary orange juice and a newspaper on boarding; dedicated Club check-in desk + priority disembarkation; cars in Club get a priority embarkation lane (commercial vehicles in Club are NOT assigned to the Club Vehicle lane)",
      "Last-minute upgrades to Club Class are possible on board — ask a Cabin Crew member, cost is minimal",
      "Single point of contact during the crossing: the Purser and the Cabin Crew",
    ],
  },
  {
    sortKey: "2026-04-25-d",
    date: "2026-04-25",
    category: "Customer Promise",
    summary: "Added the published Virtu Ferries 'no-penalties, no-hidden-fees' customer promise as a dedicated knowledge slot and standalone page. The agent must use these as the source of truth whenever passengers ask about refunds, changes, name changes, vehicle classification, baggage or booking fees, and must always quote the 'Not applicable on special offers' caveat alongside.",
    capabilities: [
      "Refunds: full refund on a paid passenger ticket if cancelled at least 24 hours prior to departure; no no-show penalty on passenger tickets",
      "Free changes: no penalties for name changes (passenger or driver & vehicle); no charges to move travel date on either leg, within the same fare basis and subject to space",
      "Vehicle classification: Land Rovers, pick-ups and panel vans up to 4.5m pay light vehicle tariffs when used non-commercially; passenger mini-vans pay light vehicle tariffs; heavily discounted rates for commercial vehicles up to 5.9m",
      "No hidden fees: no personal baggage over-weight charges, no booking admin charges, no credit card payment fees, no extra charge for new tickets bought at check-in",
      "Mandatory caveat to state alongside any of the above promises: 'Not applicable on special offers'",
    ],
  },
  {
    sortKey: "2026-04-25-c",
    date: "2026-04-25",
    category: "Excursions",
    summary: "Added a Sicily Excursions page and content slot to the brand hub. Captures the destination context Maltese travellers care about (landscape, food, heritage) plus the named excursion catalogue (5 routes split across Winter and Summer). Auto-feeds into the agent's brand prompt for Sicily-flavour copy.",
    capabilities: [
      "Sicily context: 5 million population; joined the Italian state in 1860; ever-changing landscape; characteristic hill-top towns rebuilt after the 1693 earthquake; Magna Graecia heritage",
      "Food & drink to take home: Torroncini (nougat), Pasta di Mandorla (almond cakes), Limoncello, Vino alla Mandorla, Averna (amaro)",
      "Heritage anchors: Greek Temples of Agrigento, Roman Mosaics at Piazza Armerina, Siracusa (Greek/Roman Theatres + Cathedral / former Temple of Athena in Ortygia), baroque cities of Noto/Ragusa/Modica minutes from Pozzallo, plus Palermo, Erice and Cefalu. The Iblei Mountain Range is described as picturesque; many towns in the Provincia di Ragusa are UNESCO Heritage Sites",
      "Named excursions catalogue (5 known excursions captured from the source — may be incomplete; verify the live site before publishing customer-facing claims) — Winter: 'Mt. Etna & Catania', 'Ragusa Ibla, Modica & Scicli'; Summer: 'Taormina & Mt. Etna', 'Syracuse & Marzamemi', 'Catania & Mt. Etna'",
      "Authoritative tagline available for excursion copy: 'There is only one thing we are certain of — you will enjoy Sicily'",
    ],
  },
  {
    sortKey: "2026-04-25-b",
    date: "2026-04-25",
    category: "Onboard Experience",
    summary: "Launched Satellite Wi-Fi powered by Starlink across the Malta ↔ Sicily crossing, added a Club Class subsection to Travel Info, and stood up a new Onboard Experience page in the brand hub. Stale 'Complimentary Wi-Fi onboard' USP bullet replaced with the accurate paid/Club-Class tiering.",
    capabilities: [
      "Internet & Streaming Package: €4.00 for 2 hours of full internet access including streaming services (video, music, apps)",
      "Club Class passengers receive a complimentary voucher for the 2-hour Internet & Streaming package — included in every Club Class ticket",
      "Wi-Fi is paid for standard tickets and complimentary for Club Class — never describe it as 'free Wi-Fi for everyone'",
      "Travel Info now has a dedicated Club Class section (premium saloon, lounge accessible by lift, Starlink voucher) — distinct from the existing accessibility note",
      "New Onboard Experience page (/onboard-experience) is the source of truth for connectivity, premium tiers, and onboard amenities; content auto-feeds into the agent's brand prompt",
    ],
  },
  {
    sortKey: "2026-04-25-a",
    date: "2026-04-25",
    category: "Current Offers",
    summary: "Added the Virtu Ferries commercial vehicles offer (round trip, up to 5.9m, driver included). Tiered by vehicle length with a flat ETS surcharge per tier. Sourced from virtuferries.com via the in-admin site scraper.",
    capabilities: [
      "Commercial round-trip rates (driver ticket included): up to 4.5m €413+€67.60 ETS · up to 5.0m €520+€74.60 ETS · up to 5.5m €581+€81.60 ETS · up to 5.9m €676+€88.60 ETS",
      "Second driver travels FREE — pays charges only",
      "Offer NOT applicable to refrigerated/fresh products, livestock, or live plants",
      "Treated as a current product alongside passenger offers; surfaces in the offers page, monthly planning snapshot, and the agent's auto-fed brand prompt",
    ],
  },
  {
    sortKey: "2026-04-23-a",
    date: "2026-04-23",
    category: "Travel Info / Operational Rules",
    summary: "Authoritative travel info absorbed from virtuferries.com/travel-info/5. Use these rules as ground truth when answering passenger-facing questions or writing service/info content.",
    capabilities: [
      "Booking: payment within 48h of advance reservation; bookings within 3 days of departure must be paid same day; online bookings paid immediately; unpaid reservations auto-cancelled without notice",
      "Booking info needed — passengers: full name, DOB, ID/passport number, nationality, mobile, email. Vehicles: make, model, registration",
      "Foot passenger check-in opens 2h before departure, closes 30 min before. Arrive 1h before scheduled departure",
      "Personal luggage: max 3 pieces per person, each up to 50×40×80cm (170 linear cm), stowed in baggage trolley/storage. Plus 1 hand luggage 37×45×25cm and ≤5kg carried to seat",
      "Cars: pack as much as you like (no luggage limit), incl. sports gear and family pets. No illegal/dangerous goods. Arrive 90 min before departure. Loading order is at Master/Duty Officer's discretion (safety/logistics, not first-come)",
      "Caravans charged at non-commercial light vehicle tariffs",
      "Pets — must be declared at booking, valid pet passport + vaccinations required, notify crew on boarding. Pets NOT allowed on coach transfers (except guide dogs). Recommend evening or early-morning sailings (cooler)",
      "Pet travel options (4): (1) Pet Cabin — A/C, insulated, cages provided by company, water/food by owner, charged, pre-book required, max 1 pet per cage. Cage 1: 60×80×60cm. Cages 2-4: 73×102×76.5cm. Cages 5-7: 105×115×90cm. (2) Pets in Vehicles — kept in car with windows open, first 3 pets free. (3) Pets on outside deck — cats/dogs only, leak-proof cage max 91×64×67.5cm, owner must accompany, leash + muzzle when transiting through passenger areas, charged. (4) Small pets in passenger areas — cats/dogs only in leak-proof carrier max 70×50×51.5cm, must stay inside carrier, on floor next to seat, never on tables/seats",
      "Service & Guide Dogs welcome on board. Must be certified by ADI or IGDF. Must wear identifying jacket and harness throughout voyage",
      "Pets are NOT allowed in passenger saloons/common areas (except small-pets-in-carrier rule and guide dogs). Master may permit owner to visit pet in garage/cabin during voyage with crew escort",
      "Malta pet authority: Veterinary Regulation Directorate +356 9917 0532, petstravel.mafa@gov.mt. Pozzallo: Ufficio Sanita Pubblica Veterinaria Ragusa +39 0932 234958/960/613, igiene.allevamenti@asp.rg.it",
      "Accessibility: vessel has ramps, lift, accessible toilet. Euro Class & Club Class lounges accessible by lift. Passengers with reduced mobility encouraged to notify at booking. Disability Card holders accommodated",
      "Commercial vehicles: dangerous-goods rules per IMDG Code (see Conditions of Carriage). Commercial drivers have a designated lounge",
      "Bicycles travel free, subject to space — must be declared at booking",
      "Travel info reference page: virtuferries.com/travel-info/5. Refund policy & conditions: Conditions of Carriage at virtuferries.com/terms_and_conditions/23",
    ],
  },
  {
    sortKey: "2026-04-16-k",
    date: "2026-04-16",
    category: "Current Offers",
    summary: "Saturday Night in Malta (SNF) offer extended indefinitely. Treat as a permanent weekend product, not a seasonal Jan–Apr promo.",
    capabilities: [
      "SNF offer no longer has an end date — running ongoing as of April 2026",
      "Position SNF as a year-round weekend product, not a limited-time promo (no urgency framing tied to expiry)",
      "Pricing and schedule unchanged: €57.00 per person return, 20:30 Sat departure from Pozzallo, 06:30 Sun return",
      "Hook remains 'Out Saturday night, home Sunday morning' — time arbitrage angle still leads",
    ],
  },
  {
    sortKey: "2026-04-16-j",
    date: "2026-04-16",
    category: "Posting Cadence",
    summary: "Cadence updated to 25 posts per month per platform. Instagram strategy clarified: reuse Facebook content where possible, create IG-native posts when it doesn't translate.",
    capabilities: [
      "English market: 25 Facebook posts per month",
      "Italian market: 25 Facebook posts + 25 Instagram posts per month",
      "Instagram default: cross-post from Facebook where content is image/video-led and needs no link",
      "Instagram exception: platform-specific IG post created when FB post is link-heavy, long-form, or FB-native",
      "Generate-plan now targets 25 posts per market with even daily distribution",
    ],
  },
  {
    sortKey: "2026-04-16-i",
    date: "2026-04-16",
    category: "Content Pillars",
    summary: "Content pillars updated and expanded from 4 to 5. Each pillar now has a defined goal and tone register to guide content generation.",
    capabilities: [
      "Pillar 1 — Why VF: reasons to choose Virtu Ferries; crossing as the obvious, easy choice",
      "Pillar 2 — Why Sicily: destination-led content; sells Sicily so VF is the natural next step",
      "Pillar 3 — VF Recommends: curated local travel intel; positions VF as trusted guide not ticket seller",
      "Pillar 4 — Virtu Ferries Experience: on-board, crew, UGC, social proof; real people, real crossings",
      "Pillar 5 — Sicily Experience: immersive sensory content, no hard sell; let the island do the talking",
    ],
  },
  {
    sortKey: "2026-04-16-h",
    date: "2026-04-16",
    category: "Current Offers",
    summary: "Live offer pricing loaded into the agent. The agent now knows the three active offers, their prices, schedules, and how to use them as content material.",
    capabilities: [
      "One Day Offer: adult return €63.60, child €44.60, light car €109.00, motorbike €69.00",
      "More Than One Day Offer: adult return €63.60, light car €109.00 — extended until May 30, 2026",
      "Saturday Night in Malta (SNF): €57.00 per person return, 20:30 Saturday departure from Sicily, 06:30 Sunday return — running January–April 2026",
      "Offer copywriting rules: lead with human benefit, not price; SNF hook is 'Out Saturday night, home Sunday morning'",
      "Always directs to virtuferries.com for booking — never implies guaranteed availability",
    ],
  },
  {
    sortKey: "2026-04-16-g",
    date: "2026-04-16",
    category: "Company History & Fleet",
    summary: "Company heritage, fleet specifications, and scale data loaded. The agent now knows the founding story, vessel names and dimensions, annual volumes, and how to use this history as a creative and credibility asset.",
    capabilities: [
      "Founded 1988 in Malta — 36+ years of continuous operation, one of Malta's longest-serving transport institutions",
      "Fleet: Saint John Paul II (flagship, 110m, 900 pax, Incat 089, in service March 2019) and Jean de La Valette (106.5m, 800 pax, since 2010)",
      "Scale: 250,000+ passengers and 25,000+ vehicles annually — mainstream route, not niche",
      "All vessels fly the Maltese flag — local pride asset for English market content",
      "Year-round operation flagged as a genuine differentiator vs seasonal competitors",
      "Venezia Lines subsidiary: Adriatic routes, founded 2001, seasonal April–October",
      "Heritage framing guidance: use as credibility signal, not a boast; mainstream and familiar, not discovered",
    ],
  },
  {
    sortKey: "2026-04-16-f",
    date: "2026-04-16",
    category: "Operational Knowledge",
    summary: "Full operational knowledge base loaded into the brand agent. The agent can now answer detailed passenger questions about tickets, boarding, luggage, pets, car hire, excursions, and more.",
    capabilities: [
      "Knows both ticket classes in detail: Euro Class (Upper Deck, free seating) and Club Class (Bridge Deck, priority boarding, newspaper, OJ)",
      "Full vessel layout: 5 Upper Deck lounges, Bridge Deck, Hybleum shop, bars, pet cabin, facilities for reduced mobility",
      "Travel Made Simple policy: full refund 24h before, no no-show penalty, no name change fees, no date change fees within same fare",
      "Luggage rules by passenger type: foot passengers (3 bags + 1 hand luggage), car passengers (no restrictions)",
      "Four pet travel options with size limits, booking requirements, and weather restrictions",
      "Check-in times, required documents, minor travel rules, and document validity (incl. Italian Paper ID invalid from Aug 2026)",
      "Car hire in Pozzallo: vehicle groups, pricing, CDW options, refund policy",
      "Excursions by season: Syracuse, Ragusa Ibla, Taormina, Mt. Etna, and Malta in One Day",
      "Seasonal content intelligence: demand peaks, audience travel patterns, and month-by-month content opportunities",
    ],
  },
  {
    sortKey: "2026-04-16-e",
    date: "2026-04-16",
    category: "Approval & Learning",
    summary: "Content approval queue, rejection flow, and learning loop built. Preference data now injected into agent context before every content generation call.",
    capabilities: [
      "Agent approves and rejects posts one at a time, storing decisions in PostgreSQL",
      "Aggregates approval and rejection patterns over time — pillar, tone register, format, and market",
      "Surfaces active constraints automatically when a rejection reason appears 3 or more times",
      "Injects learned preferences into every brand-guidelines and social-expert content generation call silently",
      "Monthly Learning Summary card shows approved/rejected patterns and active constraints at the top of the Social Expert page",
      "Close Month archives all pending posts and logs a changelog entry summarising the month",
    ],
  },
  {
    sortKey: "2026-04-16-d",
    date: "2026-04-16",
    category: "Trend Adaptation",
    summary: "Trend Adaptation feature added to Social Media Expert page.",
    capabilities: [
      "Receive a trend as text, link, image, or any combination — analyse the core mechanic that makes it work",
      "Assess brand fit honestly — if the trend doesn't translate to a travel/ferry brand without feeling forced, declines to produce output",
      "Return one adapted content idea per applicable market (English, Italian, or both) when fit is confirmed",
      "Each idea includes a 2–3 line concept, a one-line explanation of why it works for VF, the target market, and the specific platform",
    ],
  },
  {
    sortKey: "2026-04-16-c",
    date: "2026-04-16",
    category: "Social Media Expert",
    summary: "Social Media Expert page built. Copy review, image review, and chat interface wired to OpenAI.",
    capabilities: [
      "Review copy against brand guidelines and return a structured verdict: On Brand / Needs Work / Off Brand",
      "Provide a plain-language explanation, specific suggestions, a rewritten version, and tone notes for any copy submitted",
      "Review images for brand fit using GPT-4o vision — same structured verdict output",
      "Maintain full conversational context within a chat session — follow-up questions build on previous answers",
      "Render markdown responses cleanly: tables, bullet lists, bold text, and structured plans",
    ],
  },
  {
    sortKey: "2026-04-16-b",
    date: "2026-04-16",
    category: "Brand Foundation",
    summary: "Brand guidelines system prompt added. Brand Identity and Social Media sections populated with real content.",
    capabilities: [
      "Agent now knows the full market structure: English market (Facebook, Maltese audience) and Italian market (Facebook + Instagram, Sicilian audience)",
      "Knows all four content pillars: Why VF, Why Sicily / Why Malta, VF Recommends, VF Experience",
      "Applies the correct tone register per context: Offer, Destination Spotlight, Operational, Cultural/Seasonal, UGC",
      "Follows the monthly briefing protocol — never produces a content plan without running a structured brief first",
      "Enforces promotional lead time rules: 4–6 weeks ahead, flags missed windows before outputting any plan",
      "Adapts trends to the VF brand with honest fit assessment — never forces output",
      "Cross-posting logic for Italian market: flags Cross-post Y/N with reasoning on every content piece",
      "Applies copy rules: banned words, exclamation mark discipline, CTA only when earned, register tuned by market",
    ],
  },
  {
    sortKey: "2026-04-16-a",
    date: "2026-04-16",
    category: "Initial Setup",
    summary: "Brand hub scaffolded with OpenAI brand agent, full brand identity, tone of voice guidelines, social media strategy, and content pillars for Virtu Ferries.",
    capabilities: [
      "Answer questions about Virtu Ferries brand identity, colours, fonts, and visual language",
      "Apply and explain the brand tone of voice — travel-forward, editorially sharp, warm but not gushing",
      "Generate on-brand social media content for Instagram, Facebook, X, and LinkedIn",
      "Advise on what to say and what not to say based on established messaging guidelines",
      "Provide context on the Malta–Sicily route, crossing time, and key brand messages",
    ],
  },
];

// Gozo Highspeed (brand_id 2) — fresh slate; will fill in as we go.
export const gozoKnowledgeChangelog: ChangelogEntryStatic[] = [
  {
    sortKey: "2026-05-06-d",
    date: "2026-05-06",
    category: "Content Calendar",
    summary: "Removed the separate Edit-post step from the post detail modal — caption, time, links, pillar, format, assigned-to, visual direction, resources and notes are now editable inline straight from the detail view.",
    capabilities: [
      "Click any field in the post detail modal to edit it in place; saves automatically on blur or Enter with a tiny saving / saved indicator",
      "Pillar, Format and Assigned-to use dropdowns (Assigned-to pulls from the existing team-member list); Date and Time use native pickers; URLs (link, Drive folder, visual reference) get one-click open after save",
      "The 'Edit post' button is gone — same affordances as the legacy edit modal, no extra step",
    ],
  },
  {
    sortKey: "2026-05-06-c",
    date: "2026-05-06",
    category: "Media Library",
    summary: "Each media asset can now carry a source-file link (Canva, PSD, Figma, Drive, Dropbox, etc.) so the editable original is always one click away from the rendered visual.",
    capabilities: [
      "Add, edit or remove a source URL on any asset from the preview modal — auto-prefixes https:// and labels the link by source (Canva, Figma, Drive, …)",
      "Asset cards show a small link icon when a source file is attached, so the team can spot which visuals have an editable original",
    ],
  },
  {
    sortKey: "2026-05-06-b",
    date: "2026-05-06",
    category: "Media Library",
    summary: "Added folder organisation to the Media Library, including a built-in Evergreen folder, the ability to create custom folders on the fly, and per-asset folder reassignment from the preview modal.",
    capabilities: [
      "Group uploaded media into folders (Evergreen by default; custom folders can be created any time) and filter the grid by active folder",
      "Move any existing asset between folders from its preview modal — the change persists and the folder pill counts update live",
      "When a folder is selected, new uploads land into that folder automatically, with a clear 'Uploading into …' indicator next to the upload button",
    ],
  },
  {
    sortKey: "2026-04-25-a",
    date: "2026-04-25",
    category: "Initial Setup",
    summary: "Brand hub initialised for Gozo Highspeed. No legacy posts or knowledge yet — the agent will learn the brand voice from scratch as content is added.",
    capabilities: [
      "Recognise that Gozo Highspeed is a separate brand from Virtu Ferries",
      "Wait for brand identity, voice notes, pillars and past posts before generating opinionated content",
      "Default to neutral, factual tone until brand voice is defined",
    ],
  },
];

// Brand-id → static seed entries.
export const brandKnowledgeChangelog: Map<number, ChangelogEntryStatic[]> = new Map([
  [1, knowledgeChangelog],
  [2, gozoKnowledgeChangelog],
]);
